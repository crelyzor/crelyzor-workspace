# Crelyzor Verification Report — 2026-03-14-10-58
Verifying against: `2026-03-13-16-07-review.md`

## Verdict: NOT READY — CRITICAL ISSUES REMAIN

30 of 32 previously-fixed issues are confirmed resolved. However, 6 new CRITICAL issues were discovered (missed by the previous scan), plus 1 regression from an incomplete fix. The codebase cannot be considered production-ready until these are resolved.

---

## Fixed Issues ✅ (30 of 32)

| Issue | File | Confirmed By |
|-------|------|-------------|
| CORS hardcoded Vercel URL / LAN IP | `corsOptions.ts` | Security agent: now reads from `ALLOWED_ORIGINS` env var |
| uploadRecording missing ownership + transaction | `recordingService.ts` | DB agent: `findFirst({ createdById, isDeleted: false })` + `$transaction` |
| recordingController raw res.json + missing userId | `recordingController.ts` | API contract agent: all handlers use `globalResponseHandler` |
| aiController getSummary/getNotes ownership gaps | `aiController.ts` | Security agent: `findFirst({ createdById: userId, isDeleted: false })` guards present |
| transcriptController not scoping by userId | `transcriptController.ts` | Security agent: passes `userId` to service on all handlers |
| transcriptionService ownership + atomic writes | `transcriptionService.ts` | DB agent: `getTranscript` scoped by `createdById`; writes in `$transaction` |
| googleController JSON.parse unguarded + OAuth not in transaction | `googleController.ts` | Security agent: try/catch on parse; `$transaction` with timeout |
| cardService getPublicCard leaking userId | `cardService.ts` | Security agent: `userId` destructured out before response |
| globalErrorHandler console.error | `globalErrorHandler.ts` | Silent failures agent: all logging uses `logger.error` |
| authMiddleware console.error + empty catch + any | `authMiddleware.ts` | Silent failures agent: `logger.error/warn` used; service typed correctly |
| authController 7× console.error | `authController.ts` | Tech debt agent: all paths use `logger.error` |
| authService console.error in logLoginHistory | `authService.ts` | Silent failures agent: `logger` imported and used |
| speakerService findUnique without isDeleted | `speakerService.ts` | DB agent: uses `findFirst({ isDeleted: false })` on meeting guard |
| meetingController getMeetingById findUnique without isDeleted | `meetingController.ts` | DB agent: now `findFirst({ isDeleted: false })` — isDeleted fixed ✅ |
| publicCardRoutes no rate limiting | `publicCardRoutes.ts` | Security agent: `publicWriteLimiter` + `clickLimiter` applied |
| smaRoutes no rate limiting on AI endpoints | `smaRoutes.ts` | Security agent: `userRateLimit` applied to all AI endpoints |
| aiService module-level new OpenAI() | `aiService.ts` | Security agent: all calls go through `getOpenAIClient()` |
| aiService catch blocks swallowing DB write errors | `aiService.ts` | Silent failures agent: DB errors now propagate outside catch |
| aiService Promise.all 4 items / 3 destructured | `aiService.ts` | DB agent: 4th result documented as intentional fire-and-forget |
| aiService extractTasks sequential loop | `aiService.ts` | DB agent: `tx.task.createMany` inside `$transaction` with 15s timeout |
| meetingService updateMeeting/cancelMeeting/completeMeeting without isDeleted | `meetingService.ts` | DB agent: all use `findFirst({ isDeleted: false })` |
| cardService updateCard 3 sequential writes | `cardService.ts` | DB agent: wrapped in `$transaction` — ⚠️ see regression below |
| cardService exportContacts unbounded findMany | `cardService.ts` | DB agent: `take: 10000` cap applied |
| transcriptionService post-transcription writes not atomic | `transcriptionService.ts` | DB agent: confirmed covered by same fix as #6 |
| aiService askAI no transcript token cap | `aiService.ts` | Performance agent: `MAX_TRANSCRIPT_CHARS = 20000` enforced |
| cards-frontend wrong fallback port 4000 | `cards-frontend/src/lib/api.ts` | Tech debt agent: defaults to `localhost:3000` |
| useMeetingQueries all mutations missing onError | `useMeetingQueries.ts` | Frontend agent: all 7 mutations have `onError: toast.error(...)` |
| useCardQueries missing onError + staleTime + hardcoded key | `useCardQueries.ts` | Frontend agent: all fixed |
| useIntegrationQueries hardcoded key + missing staleTime + onError | `useIntegrationQueries.ts` | Frontend agent: uses `queryKeys.auth.sessions()`; staleTime 30s; onError present |
| queryKeys.ts missing sessions/templates keys | `queryKeys.ts` | Frontend agent: `auth.sessions()` and `cards.templates()` added |
| MeetingDetail.tsx no isError state | `MeetingDetail.tsx` | Frontend agent: dedicated `isError` branch with back button |
| useTagQueries useUserTags missing staleTime | `useTagQueries.ts` | Performance agent: `staleTime: 5 * 60 * 1000` |

---

## Still Present ❌ (1)

| Issue | File | Severity | Original Report Reference |
|-------|------|----------|--------------------------|
| getMeetingById ownership scope at DB level | `meetingController.ts` line 166 | HIGH | Issue #14 — isDeleted was fixed, but `createdById: userId` was never added to the Prisma WHERE clause. Post-fetch JS ownership check remains the pattern. |

---

## Regressions ⚠️ (1)

| Issue | File | Severity | What It Broke |
|-------|------|----------|---------------|
| updateCard transaction uses bare `prisma` not `tx` inside callback | `cardService.ts` line 288 | HIGH | The fix for Issue #22 (updateCard transaction) introduced a new bug: the `prisma.user.findUnique` call for HTML regeneration inside the transaction callback uses the module-level `prisma` client instead of `tx`. This means the user lookup runs outside the transaction boundary and is not rolled back if the subsequent `tx.card.update` fails. |

---

## New Issues Found 🆕 (41 total)

### CRITICAL (6)

| Issue | File | Line(s) | Description |
|-------|------|---------|-------------|
| Global Express error handler uses `console.error` + wrong response shape | `app.ts` | 29–38 | Every unhandled exception bypasses the structured logger and returns `{ error: { message, status } }` instead of the standard API shape |
| 404 handler uses raw `Error` with `any` cast | `app.ts` | 23–27 | Creates a non-standard error response that bypasses `globalErrorHandler` |
| getMeetingById fetches without `createdById: userId` scope | `meetingController.ts` | 166, 197–205 | Ownership enforced post-fetch via JS check; DB query is unscoped by user |
| deleteMeeting uses `findUnique` with no `isDeleted: false` | `meetingController.ts` | 222–231 | Soft-deleted meetings can be re-deleted; no ownership in DB WHERE clause |
| aiController + speakerController use raw `res.status().json()` | `aiController.ts`, `speakerController.ts` | 68, 104, 218–220, 233, 17, 47 | Response shape inconsistent with rest of API; no try/catch means errors go to `console.error` handler |
| Open redirect in OAuth callback exposes auth tokens | `googleController.ts` | 15, 95 | `redirectUrl` from state not validated against domain allowlist — attacker can craft state to capture JWT tokens |

### HIGH (18)

| Issue | File | Line(s) | Description |
|-------|------|---------|-------------|
| getUserCards no `isDeleted: false` | `cardService.ts` | 178 | Soft-deleted cards returned in user's card list |
| getCardById `findUnique` with no `isDeleted` + post-fetch ownership | `cardService.ts` | 192 | Any authenticated user who knows a card ID can fetch it pre-check |
| updateCard `findUnique` with no `isDeleted` + post-fetch ownership | `cardService.ts` | 210 | Same pattern as getCardById |
| updateCard uses bare `prisma` not `tx` inside transaction | `cardService.ts` | 288 | User lookup outside transaction boundary (also the regression above) |
| deleteCard two sequential writes outside transaction | `cardService.ts` | 324–344 | `card.delete` + `card.update` for default promotion can leave inconsistent state |
| duplicateCard writes not in transaction | `cardService.ts` | 362–397 | `card.create` + `card.update` for HTML can leave incomplete card in DB |
| getNotes findMany no `take` limit | `aiController.ts` | 122 | Unbounded MeetingNote query |
| generateSummary `findUnique` no `isDeleted: false` | `aiService.ts` | 32 | Fetches deleted meetings for summary generation |
| processTranscriptWithAI no ownership check on meeting | `aiService.ts` | 286–305 | No guard that meetingId belongs to a valid non-deleted meeting |
| getMeetingsWithoutPagination hard-coded `take: 1000` with heavy include | `meetingService.ts` | 496–501 | 1000 meetings × full participants + stateHistory + tags = OOM risk |
| stateHistory in meetingInclude is unbounded (unused by frontend) | `meetingService.ts` | 11–31 | Full state history rows fetched on every meeting list and detail query |
| Participant conflict checks are sequential instead of Promise.all | `meetingService.ts` | 224–239 | O(n) serial DB round-trips during updateMeeting |
| Content generation (MEETING_REPORT, BLOG_POST, EMAIL) no char cap | `aiService.ts` | 450–460 | Full transcript text sent to OpenAI — context overflow + cost risk |
| generateSummary and extractKeyPoints no char cap | `aiService.ts` | 24–76, 82–140 | Same issue — full transcript to OpenAI in processTranscriptWithAI pipeline |
| extractTasks no char cap | `aiService.ts` | 152–226 | Same issue |
| OAuth `redirectUrl` not validated against domain allowlist | `googleController.ts` | 15, 95 | Token exfiltration via crafted redirect (same as CRITICAL but listed for completeness at this severity by security agent) |
| getRecordings no `isDeleted` filter and no `take` limit | `recordingService.ts` | 174–177 | Soft-deleted recordings returned; unbounded result set |
| Empty catch in logout swallows non-token errors | `authService.ts` | 43–45 | `PrismaClientKnownRequestError` and other errors silently discarded |

### MEDIUM (17)

| Issue | File | Line(s) | Description |
|-------|------|---------|-------------|
| Storage controller endpoints bypass Zod validation | `storage.controller.ts` | all endpoints | No Zod schemas on any input |
| Card controller: slug, query params, tags, days not Zod validated | `cardController.ts` | 162, 191–197, 215–217, 276 | Raw `req.body` / `req.query` with manual checks only |
| sessionId path param not UUID-validated | `authController.ts` | 129 | Inconsistent with `transcriptController` which validates |
| `catch (err: any)` in setUsername | `authController.ts` | 277 | Overly broad catch with `any` type |
| userRateLimit uses in-memory Map | `authMiddleware.ts` | 131–132 | Resets on server restart; not multi-instance safe |
| Empty summary string saved without warning | `aiService.ts` | 59 | Falsy OpenAI content cached as successful generation |
| useMeetings, useMeetingsAll, useMeeting, useVoiceNotes no staleTime | `useMeetingQueries.ts` | multiple | Refetches on every mount and window focus |
| SMA queries no staleTime (transcript, summary, tasks, notes, recordings, speakers) | `useSMAQueries.ts` | multiple | Refetches on every tab switch in meeting detail |
| useAttachmentQueries no staleTime | `useAttachmentQueries.ts` | multiple | Unnecessary refetches on mount |
| useCardAnalytics no staleTime | `useCardQueries.ts` | 45–51 | Analytics refetch on every page mount |
| Prisma `where` clause cast to `any` for date filtering | `meetingService.ts` | 460–470 | Type safety gap in hot query path |
| Prisma enum values cast with `as any` | `meetingController.ts` | 67, 89, 110, 111, 145, 146 | Should use `import { MeetingStatus } from '@prisma/client'` |
| AskAITab AbortController never aborted on unmount | `SharedTabs.tsx` | ~1184–1363 | Active SSE stream leaks when user navigates away mid-stream |
| SegmentRow not wrapped in React.memo | `SharedTabs.tsx` | ~282 | All segments re-render on every 3s poll tick during transcription |
| "gpt-4o-mini" hardcoded 6×, "nova-2" hardcoded once | `aiService.ts`, `transcriptionService.ts` | multiple | Model upgrades require touching 7 files |
| GCS bucket fallback to "sma-dev" when env var missing | `storage.service.ts` | 4 | Silent wrong-bucket usage in production |
| `VITE_CARDS_BASE_URL` vs `VITE_CARDS_PUBLIC_URL` inconsistency | `ShareSheet.tsx`, `emailSignature.ts` | 38 | One env var reference will always be undefined |

### LOW (7)

| Issue | File | Line(s) | Description |
|-------|------|---------|-------------|
| Analytics `days` param and contacts pagination have no upper bound | `cardController.ts` | 193–195, 276 | No max enforced |
| meetingId not UUID-validated before DB query | `meetingController.ts` | 164 | Inconsistent with transcriptController |
| `getDeviceInfo` imported but never called | `authController.ts` | 16 | Dead import |
| `apiResponse` imported but never called | `googleController.ts` | 3 | Dead import |
| Default tag color `"#6b7280"` hardcoded | `tagService.ts` | ~33 | Should be a named constant |
| Dev CORS origins always present (no NODE_ENV guard) | `corsOptions.ts` | 3–7 | Low risk but localhost origins valid in production |
| No `error.tsx` in cards-frontend Next.js root layout | `cards-frontend/src/app/` | — | Unhandled errors show browser default page |

---

## Recommended Next Action

Run `/crelyzor-review` again. The following must be addressed before production:

**Highest urgency (fix first):**
1. `app.ts` — Replace the raw Express error handler with `globalErrorHandler` + replace 404 handler
2. `googleController.ts` — Add `redirectUrl` allowlist validation before OAuth redirect
3. `aiController.ts` / `speakerController.ts` — Replace all `res.status().json()` calls with `apiResponse()`
4. `cardService.ts` — Add `isDeleted: false` to `getUserCards`, `getCardById`, `updateCard`, `deleteCard`, `duplicateCard`; move ownership into DB WHERE clauses; fix `prisma` → `tx` inside updateCard transaction; wrap deleteCard and duplicateCard in transactions
5. `aiService.ts` — Apply `MAX_TRANSCRIPT_CHARS` cap to `generateSummary`, `extractKeyPoints`, `extractTasks`, and all `CONTENT_PROMPTS` entries
6. `meetingController.ts` — Add `createdById: userId` to `getMeetingById` and `deleteMeeting` Prisma queries

**After CRITICAL/HIGH are resolved:**
- Add `staleTime` to all meeting, SMA, and attachment queries
- Add `React.memo` to `SegmentRow`
- Fix AbortController cleanup in `AskAITab`
- Extract AI model name constants
- Add Zod schemas to storage and card controllers
