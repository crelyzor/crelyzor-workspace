# Crelyzor Production Review — 2026-03-17

## Summary
- Total issues found: 32 (4 critical, 11 high, 12 medium, 5 low)
- Total issues fixed: 22
- Issues skipped: 10 (schema-blocked or false positives — listed below)
- Plan approval: Proceeded after 2 iterations (max reached with outstanding feedback incorporated)

---

## Issues Found & Fixed

### CRITICAL

| # | File | Issue | Status |
|---|------|-------|--------|
| C-1 | `calendar-backend/src/controllers/storage.controller.ts` | Ownership check used substring match (`path.includes(userId)`) allowing path traversal — attacker with userId "abc" could access "xyzabc/..." | ✅ Fixed — changed to exact path-segment match `path.split('/').includes(userId)` |
| C-2 | `calendar-backend/src/routes/smaRoutes.ts` | `POST /meetings/:meetingId/process-ai` had no rate limiting — AI endpoint exposed to abuse | ✅ Fixed — added `userRateLimit(10, 60 * 60 * 1000)` |
| C-3 | `calendar-backend/src/services/cardService.ts` | `updateContactTags` used `findUnique({ where: { id } })` without userId — no ownership verification | ✅ Fixed — changed to `findFirst({ where: { id, userId } })` |
| C-4 | `calendar-backend/src/services/cardService.ts` | `deleteContact` used `findUnique({ where: { id } })` without userId — no ownership verification | ✅ Fixed — changed to `findFirst({ where: { id, userId } })` |

### HIGH

| # | File | Issue | Status |
|---|------|-------|--------|
| H-1 | `calendar-backend/src/utils/globalErrorHandler.ts` | Error middleware had 3 params — Express only recognizes error middleware with exactly 4 params | ✅ Fixed — added `_next?: NextFunction` as optional 4th param |
| H-2 | `calendar-backend/src/utils/globalErrorHandler.ts` | `InternalServerError` constructor had `error?: any` — unsafe type | ✅ Fixed — changed to `error?: unknown` |
| H-3 | `calendar-backend/src/services/auth/tokenService.ts` | JWT decode result cast as `any` — `exp` field access unsafe | ✅ Fixed — cast to `{ exp?: number } \| null` |
| H-4 | `calendar-backend/src/worker/jobProcessor.ts` | `findUnique({ id })` missing `isDeleted: false` on Meeting (Meeting DOES have isDeleted) | ✅ Fixed — changed to `findFirst({ where: { id, isDeleted: false } })` |
| H-5 | `calendar-backend/src/services/speaker/speakerService.ts` | `renameSpeaker` used `findUnique({ id })` without meetingId ownership scope | ✅ Fixed — changed to `findFirst({ where: { id, meetingId } })` |
| H-6 | `calendar-backend/src/services/shareService.ts` | `MeetingSpeaker.findMany` on public endpoint had no `take` limit — unbounded query | ✅ Fixed — added `take: 100` |
| H-7 | `calendar-backend/src/services/ai/aiService.ts` | `generateMeetingTitle` catch block swallowed both OpenAI failure and DB write failure — silently exits | ✅ Fixed — split into two try/catch blocks with separate error logging |
| H-8 | `calendar-backend/src/services/ai/aiService.ts` | `askAI` loaded ALL transcript segments with no limit — could OOM on large transcripts | ✅ Fixed — added `take: 500` to segments include |
| H-9 | `crelyzor-frontend/src/pages/meeting-detail/VoiceNoteDetail.tsx` | No ErrorBoundary on any of 7 tab sections — one render crash kills entire page | ✅ Fixed — wrapped all 7 `<CardContent>` sections in `<ErrorBoundary>` |
| H-10 | `crelyzor-frontend/src/pages/meeting-detail/ScheduledDetail.tsx` | No ErrorBoundary on any of 8 `<TabsContent>` sections | ✅ Fixed — wrapped all 8 in `<ErrorBoundary>` |
| H-11 | `crelyzor-frontend/src/services/meetingsService.ts` | `listAll` typed as returning `Meeting[]` but backend returns `{ meetings, truncated }` — type mismatch | ✅ Fixed — typed as `{ meetings: Meeting[]; truncated: boolean }` |

### MEDIUM

| # | File | Issue | Status |
|---|------|-------|--------|
| M-1 | `calendar-backend/src/services/ai/aiService.ts` | `getGeneratedContents` findMany had no `take` limit | ✅ Fixed — added `take: 50` |
| M-2 | `calendar-backend/src/controllers/authController.ts` | Dead `getAuthStatus` method — never routed, exposed user email if accidentally wired | ✅ Fixed — removed |
| M-3 | `calendar-backend/src/utils/database/prisma.ts` | Orphaned second PrismaClient instance — nothing imported it, wasted connection pool slot | ✅ Fixed — deleted file |
| M-4 | `crelyzor-frontend/src/hooks/queries/useMeetingQueries.ts` | `useMeetings`, `useMeeting`, `useMeetingsAll`, `useVoiceNotes` missing `staleTime` | ✅ Fixed — added appropriate staleTime to all 4 |
| M-5 | `crelyzor-frontend/src/hooks/queries/useMeetingQueries.ts` | `useMeetingsAll` and `useVoiceNotes` not unwrapping `.meetings` from paginated response | ✅ Fixed — updated to unwrap `result.meetings` |
| M-6 | `crelyzor-frontend/src/hooks/queries/useSMAQueries.ts` | `useRecordings` and `useSpeakers` missing `staleTime` | ✅ Fixed — added `staleTime: 60_000` and `5 * 60_000` |
| M-7 | `crelyzor-frontend/src/hooks/queries/useCardQueries.ts` | `useCardAnalytics` and `useCardContacts` missing `staleTime` | ✅ Fixed — added `staleTime: 5 * 60_000` and `30_000` |
| M-8 | `crelyzor-frontend/src/hooks/queries/useUserQueries.ts` | `useUpdateProfile` mutation missing `onError` handler — silent failure on profile save | ✅ Fixed — added `onError` with `toast.error` |
| M-9 | `crelyzor-frontend/src/pages/home/RecentVoiceNotes.tsx` | `isError` not handled — network failure shows empty state instead of error message | ✅ Fixed — added error state block |
| M-10 | `crelyzor-frontend/src/pages/meeting-detail/ShareSheet.tsx` | `mutateAsync` not wrapped in try/catch — unhandled promise rejection on share update failure | ✅ Fixed — wrapped both `mutateAsync` calls in try/catch |
| M-11 | `crelyzor-frontend/src/pages/auth-callback/AuthCallback.tsx` | OAuth failure navigated to signin silently — no user-visible error message | ✅ Fixed — added `toast.error` with OAuth error context |
| M-12 | `crelyzor-public/src/components/CardView.tsx` | Hardcoded fallback `localhost:4000` — backend runs on `localhost:3000` | ✅ Fixed — changed to `localhost:3000` |

### LOW

| # | File | Issue | Status |
|---|------|-------|--------|
| L-1 | Multiple controllers | UUID path params not validated with Zod (cardController, recordingController, aiController) | ⏭️ Skipped — low risk, would require adding Zod refine to many routes; defer to next review |
| L-2 | `calendar-backend/src/services/meetingService.ts` | `participants` Prisma include fetches full object; only userId/participantType needed | ⏭️ Skipped — performance improvement, not blocking; defer to optimization pass |
| L-3 | `calendar-backend/src/controllers/meetingController.ts` | `as any` casts for MeetingStatus enum alignment | ⏭️ Skipped — requires deeper type alignment with Prisma-generated enum; defer |
| L-4 | Various | console.log in non-critical paths | ⏭️ Skipped — none found in security-critical paths; logger convention already enforced |
| L-5 | Various | Unused imports in some component files | ⏭️ Skipped — TS compiler will catch at build time; low urgency |

---

## Issues Skipped (Schema-Blocked or False Positives)

| Issue | Reason |
|-------|--------|
| Missing `isDeleted: false` on MeetingRecording queries | MeetingRecording model has NO `isDeleted` field in schema — cannot filter |
| Missing `isDeleted: false` on MeetingTranscript queries | MeetingTranscript model has NO `isDeleted` field in schema |
| Missing `isDeleted: false` on MeetingSpeaker queries (`getSpeakers`) | MeetingSpeaker model has NO `isDeleted` field in schema |
| Missing `isDeleted: false` on MeetingNote queries | MeetingNote model has NO `isDeleted` field in schema |
| Missing `isDeleted: false` on CardContact queries | CardContact model has NO `isDeleted` field in schema |
| Missing `isDeleted: false` on Card queries (tagService) | Card model has NO `isDeleted` field in schema |
| `deleteContact` hard delete instead of soft delete | CardContact has no `isDeleted` field — hard delete is only option; acknowledged explicitly |
| Data leak of `isDeleted`/`deletedAt`/`deletedBy` from Card public endpoint | FALSE POSITIVE — Card model does NOT have these fields |
| `getAuthStatus` route security issue | Method was dead code with no route registration — removed entirely |
| `cardsService.ts exportContacts` response type | `apiClient.get<string>` for CSV — verified backend sends Content-Disposition; browser handles correctly |

---

## Files Changed

**calendar-backend:**
- `src/routes/smaRoutes.ts`
- `src/services/cardService.ts`
- `src/controllers/storage.controller.ts`
- `src/utils/globalErrorHandler.ts`
- `src/services/auth/tokenService.ts`
- `src/worker/jobProcessor.ts`
- `src/services/ai/aiService.ts`
- `src/services/speaker/speakerService.ts`
- `src/services/shareService.ts`
- `src/controllers/authController.ts` (dead method removed)
- `src/utils/database/prisma.ts` (deleted — orphaned file)

**calendar-frontend (crelyzor-frontend):**
- `src/services/meetingsService.ts`
- `src/hooks/queries/useMeetingQueries.ts`
- `src/hooks/queries/useSMAQueries.ts`
- `src/hooks/queries/useCardQueries.ts`
- `src/hooks/queries/useUserQueries.ts`
- `src/pages/home/RecentVoiceNotes.tsx`
- `src/pages/meeting-detail/ShareSheet.tsx`
- `src/pages/meeting-detail/VoiceNoteDetail.tsx`
- `src/pages/meeting-detail/ScheduledDetail.tsx`
- `src/pages/auth-callback/AuthCallback.tsx`

**cards-frontend (crelyzor-public):**
- `src/components/CardView.tsx`

---

## Next Steps (Manual Actions Required)

1. **Schema migration needed** — Add `isDeleted` + `deletedAt` fields to `MeetingRecording`, `MeetingTranscript`, `MeetingSpeaker`, `MeetingNote`, `CardContact`, and `Card` models if soft-delete protection is desired on these entities. Run `/migrate` after updating schema.

2. **UUID validation** — Add Zod `.uuid()` validation to path params in cardController, recordingController, and aiController routes to prevent invalid ID lookups.

3. **Participants over-fetching** — Optimize `meetingService.ts` participants include to select only `userId` and `participantType` instead of full object.

4. **MeetingStatus enum drift** — Align `MeetingStatus` TypeScript type with Prisma-generated enum to eliminate `as any` casts in meetingController.

5. **Test the AI rate limit** — Verify `process-ai` endpoint correctly enforces the 10/hour limit per user in staging.
