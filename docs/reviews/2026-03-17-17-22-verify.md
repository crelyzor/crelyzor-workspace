# Crelyzor Verification Report — 2026-03-17-17-22
Verifying against: `2026-03-17-03-39-review.md`

## Verdict: NOT READY — CRITICAL ISSUES REMAIN

---

## Fixed Issues ✅ (21 of 23 previous issues confirmed fixed)

### Critical (4/4 confirmed)
| Issue | File | Confirmed By |
|-------|------|--------------|
| C-1: Storage path traversal (path.includes → path.split('/').includes) | `storage.controller.ts` | Security agent: PASS explicitly noted |
| C-2: Rate limiting on POST /meetings/:meetingId/process-ai | `routes/smaRoutes.ts` | Security agent: userRateLimit(10, 60*60*1000) confirmed |
| C-3: updateContactTags findUnique → findFirst with userId | `services/cardService.ts:633` | Security agent + DB agent |
| C-4: deleteContact findUnique → findFirst with userId | `services/cardService.ts:650` | Security agent + DB agent |

### High (10/11 confirmed)
| Issue | File | Confirmed By |
|-------|------|--------------|
| H-1: Error middleware 3→4 params (_next?: NextFunction) | `utils/globalErrorHandler.ts:113` | Silent failures agent |
| H-2: InternalServerError error?: any → unknown | `utils/globalErrorHandler.ts:74` | Silent failures agent |
| H-3: JWT decode cast as any → { exp?: number } \| null | `services/auth/tokenService.ts:128` | Silent failures agent |
| H-4: jobProcessor findUnique → findFirst with isDeleted: false | `worker/jobProcessor.ts:34` | DB agent |
| H-5: renameSpeaker missing meetingId scope | `services/speaker/speakerService.ts:33` | DB + security agents |
| H-6: MeetingSpeaker.findMany take: 100 added | `services/shareService.ts:160` | DB agent |
| H-8: askAI take: 500 on segments | `services/ai/aiService.ts:394` | DB + performance agents |
| H-9: VoiceNoteDetail ErrorBoundaries on all 7 tab sections | `pages/meeting-detail/VoiceNoteDetail.tsx` | Frontend + silent failure agents |
| H-10: ScheduledDetail ErrorBoundaries on all 8 TabsContent | `pages/meeting-detail/ScheduledDetail.tsx` | Frontend + silent failure agents |
| H-11: meetingsService listAll typed as { meetings, truncated } | `services/meetingsService.ts:43` | API contract + frontend agents |

### Medium (11/12 confirmed)
| Issue | File | Confirmed By |
|-------|------|--------------|
| M-1: getGeneratedContents take: 50 | `services/ai/aiService.ts:562` | DB + performance agents |
| M-2: Dead getAuthStatus method removed | `controllers/authController.ts` | Security agent |
| M-3: Orphaned PrismaClient deleted | (deleted file) | DB agent |
| M-4: staleTime added to useMeetings, useMeeting, useMeetingsAll, useVoiceNotes | `hooks/queries/useMeetingQueries.ts` | Frontend + performance agents |
| M-5: useMeetingsAll and useVoiceNotes unwrap .meetings | `hooks/queries/useMeetingQueries.ts:28,146` | Frontend + API contract agents |
| M-6: useRecordings and useSpeakers staleTime added | `hooks/queries/useSMAQueries.ts` | Performance agent |
| M-7: useCardAnalytics and useCardContacts staleTime added | `hooks/queries/useCardQueries.ts:50,64` | Performance agent |
| M-8: useUpdateProfile onError added | `hooks/queries/useUserQueries.ts:14` | Frontend + silent failure agents |
| M-9: RecentVoiceNotes isError handled | `pages/home/RecentVoiceNotes.tsx:58,66,76` | Frontend agent |
| M-11: AuthCallback OAuth failure shows toast before navigate | `pages/auth-callback/AuthCallback.tsx:30–35` | Silent failures agent |
| M-12: CardView localhost:4000 → localhost:3000 | `(crelyzor-public)` | Tech debt agent (no localhost:4000 found) |

---

## Regressions ⚠️ (2 incomplete fixes)

| Issue | File | Severity | What Happened |
|-------|------|----------|---------------|
| H-7: generateMeetingTitle catch split — DB write failure still swallowed | `services/ai/aiService.ts:280` | HIGH | The two-try/catch split was applied but the inner catch on the DB write failure logs and exits silently. The caller uses the return value to respond to the user; if the DB write failed, the user sees a new title that was never saved. On next reload the old title reappears. |
| M-10: ShareSheet.tsx mutateAsync wrapped in try/catch — but catch blocks are empty | `pages/meeting-detail/ShareSheet.tsx:149,159` | MEDIUM | try/catch is present but both catch blocks are `// onError in useUpdateShare handles the toast`. mutateAsync re-throws after onError, so empty catch causes a suppressed unhandled promise rejection in the browser. User feedback works (toast fires via onError) but the rejection pattern is incorrect. |

---

## Still Present ❌ (from deferred/skipped list)

| Issue | File | Severity | Original Report Reference |
|-------|------|----------|--------------------------|
| L-1: UUID path param validation missing | Multiple controllers | HIGH (upgraded) | Deferred as LOW — now flagged HIGH by security agent |
| L-2: Participants over-fetching on meeting list | `services/meetingService.ts` | HIGH | Deferred — now expanded by performance agent |
| L-3: MeetingStatus as any casts | `controllers/meetingController.ts:77,99,120,121,156,157` | HIGH | Deferred as LOW — now 6 confirmed casts |
| L-5: Unused imports / dead code | Multiple | MEDIUM | Deferred — now expanded to dead files and dead exports |

---

## New Issues Found 🆕

### CRITICAL — Must fix before production

| # | Issue | File | Recommendation |
|---|-------|------|----------------|
| 1 | **Refresh token in OAuth redirect URL as plaintext query param** — captured by logs, proxies, browser history | `controllers/googleController.ts:116` | Use one-time code exchange via Redis instead of token in URL |
| 2 | **Card.isDeleted used in service queries but field does not exist in Prisma schema** — will cause runtime errors or silently return wrong data | `services/cardService.ts` + `prisma/schema.prisma` | Add isDeleted/deletedAt to Card schema via migration |
| 3 | **audioCompression silently returns original file on ffmpeg failure** — operator never knows compression is broken | `utils/audio/audioCompression.ts:53–58` | Re-throw or propagate error; let caller decide fallback policy |
| 4 | **Queue failure caught in uploadRecording — success response returned** — recording saved, transcription silently never queued | `services/recording/recordingService.ts:119` | Throw AppError after status revert so client receives 500 |
| 5 | **extractKeyPoints and extractTasks parse failures return [] silently** — permanently stored as "no results found" | `services/ai/aiService.ts:122,200` | Log raw content; propagate error or store a parse-failed sentinel |
| 6 | **ErrorBoundary has no componentDidCatch** — rendering crashes are invisible to the team | `components/ErrorBoundary.tsx` | Implement componentDidCatch with console.error (+ Sentry when added) |
| 7 | **MeetingNote.author is raw String @db.Uuid with no Prisma relation** — ORM cannot join on it | `prisma/schema.prisma` | Rename to userId and add proper User relation |
| 8 | **MeetingRecording.uploadedBy raw UUID with no Prisma relation** | `prisma/schema.prisma` | Add uploader User @relation |
| 9 | **MeetingRecording.fileSize typed as Int** — overflows for files >2 GB | `prisma/schema.prisma` | Change to BigInt |
| 10 | **User.deletedById raw UUID with no Prisma relation** | `prisma/schema.prisma` | Add self-relation or document as intentional loose audit |
| 11 | **getRecordings findMany on MeetingRecording with no isDeleted filter** — deleted recordings returned with active GCS signed URLs | `services/recording/recordingService.ts:179` | Add isDeleted: false filter once field added to schema |
| 12 | **taskController findMany on Task unbounded (no take limit)** | `controllers/taskController.ts:24` | Add take: 500 |
| 13 | **verifyCardOwnership missing isDeleted: false** — tag ops allowed on soft-deleted cards | `services/tagService.ts:173` | Add isDeleted: false to where clause |
| 14 | **Hardcoded personal URL https://cal.harsh.dev/book/harsh in production build** | `components/toolbar/Toolbar.tsx:29` | Replace with env var or user profile field |
| 15 | **Inactive card accessible via slug on public endpoint** — isActive filter missing on slug lookup | `services/cardService.ts:460` | Add isActive: true to slug findFirst where clause |

### HIGH — Fix before launch

| # | Issue | File | Severity |
|---|-------|------|----------|
| 1 | exportContacts cardId query param not UUID-validated | `controllers/cardController.ts:260` | HIGH |
| 2 | Multiple path params not UUID-validated (recordingController, transcriptController, taskController, speakerController, aiController) | Multiple controllers | HIGH |
| 3 | revokeSession two writes without transaction — partial failure leaves inconsistent state | `services/auth/sessionService.ts:121–134` | HIGH |
| 4 | refreshTokens not atomic — token rotation can partially succeed | `services/auth/sessionService.ts:86–107` | HIGH |
| 5 | Many MeetingTranscript, MeetingAISummary, MeetingSpeaker queries missing isDeleted filter | aiService, exportService, shareService, speakerService | HIGH |
| 6 | apiClient token refresh empty catch — silent redirect, no logging | `lib/apiClient.ts:74` | HIGH |
| 7 | Server starts in degraded state when Redis/Bull queue init fails | `index.ts:37` | HIGH |
| 8 | getMeetings list over-fetches full participant objects | `services/meetingService.ts` | HIGH |
| 9 | renderCardHtml called inside Prisma transaction in updateCard and duplicateCard | `services/cardService.ts` | HIGH |
| 10 | getCardAnalytics loads all CardView rows into memory for aggregation | `services/cardService.ts:756` | HIGH |
| 11 | exportService has no segment row limit | `services/exportService.ts:14–18` | HIGH |
| 12 | exportContacts take: 10000 too high | `services/cardService.ts:666` | HIGH |
| 13 | processTranscriptWithAI makes 5 DB reads including redundant meeting re-fetch | `services/ai/aiService.ts:300` | HIGH |
| 14 | meetingController uses manual try/catch calling local error handler — bypasses global middleware | `controllers/meetingController.ts` | HIGH |
| 15 | authController double-logs every auth error | `controllers/authController.ts` | HIGH |
| 16 | speakerController useless catch-and-rethrow double-logs speaker errors | `controllers/speakerController.ts` | HIGH |
| 17 | Cards.tsx two setTimeout calls untracked, not cleaned on unmount | `pages/cards/Cards.tsx:636,662` | HIGH |
| 18 | CardAnalytics.tsx error state has no retry button | `pages/card-analytics/CardAnalytics.tsx:34` | HIGH |
| 19 | Meetings.tsx error state has no retry button | `pages/meetings/Meetings.tsx:488` | HIGH |
| 20 | GET /meetings missing total count in pagination response | `controllers/meetingController.ts` | HIGH |
| 21 | MeetingStatus Zod enum accepts only 3 of 7 valid values (PENDING_ACCEPTANCE, ACCEPTED, etc. rejected) | `validators/meetingSchema.ts` | HIGH |
| 22 | regenerateSummary response missing id field expected by frontend | API contract drift | HIGH |
| 23 | Card.isDeleted and deletedAt leak in public card response | `services/cardService.ts:487` | HIGH |
| 24 | SetupPage username check failure silently disables submit button | `pages/setup/SetupPage.tsx:56–63` | HIGH |
| 25 | useLogout has no onError — logout API failure silent | `hooks/queries/useAuthQueries.ts` | HIGH |

### MEDIUM

| # | Issue | File | Severity |
|---|-------|------|----------|
| 1 | TOCTOU ownership check in updateMeeting, cancelMeeting, completeMeeting | `services/meetingService.ts:185,332,383` | MEDIUM |
| 2 | getClientIP blindly trusts x-forwarded-for | `middleware/authMiddleware.ts:196` | MEDIUM |
| 3 | app.ts leaks raw error messages in production responses | `app.ts:46` | MEDIUM |
| 4 | Meeting.guests type drift — frontend expects, backend never returns | Type files | MEDIUM |
| 5 | Meeting.transcriptionStatus type drift | Type files | MEDIUM |
| 6 | SMATranscript.fullText should be explicitly selected | Service files | MEDIUM |
| 7 | queryClient global retry policy retries 4xx errors | `lib/queryClient.ts` | MEDIUM |
| 8 | attachmentController req.user! non-null assertion crashes instead of 401 | `controllers/attachmentController.ts` | MEDIUM |
| 9 | Settings.tsx "Delete Account" button has no onClick handler | `pages/settings/Settings.tsx:407` | MEDIUM |
| 10 | SSE parse errors silently ignored — stream hangs forever if backend sends malformed data | `services/smaService.ts:322` | MEDIUM |
| 11 | Multiple schema models missing isDeleted/deletedAt (17 models) | `prisma/schema.prisma` | MEDIUM |
| 12 | Multiple schema models missing standard timestamps | `prisma/schema.prisma` | MEDIUM |
| 13 | Long string fields missing @db.Text | `prisma/schema.prisma` | MEDIUM |
| 14 | useSMAQueries shows generic error messages, ignores ApiError shape | `hooks/queries/useSMAQueries.ts` | MEDIUM |
| 15 | useAttachments hook missing staleTime | `hooks/queries/useAttachmentQueries.ts` | MEDIUM |
| 16 | TranscriptTab no React.memo — 5000 segment re-renders on parent state change | `pages/meeting-detail/SharedTabs.tsx` | MEDIUM |
| 17 | CardContacts bulk delete handleBulkDelete no onError — count stalls on failure | `pages/cards/CardContacts.tsx` | MEDIUM |
| 18 | Settings.tsx SecuritySection sessions loading/error state not handled | `pages/settings/Settings.tsx` | MEDIUM |
| 19 | Dead files: deleteUserSchema.ts, deleteUserTypes.ts | `validators/`, `types/` | MEDIUM |
| 20 | Dead API methods in meetingsService.ts (5 Phase 2 stubs) | `services/meetingsService.ts` | MEDIUM |
| 21 | CARDS_PUBLIC_URL fallback duplicated 3 times | Multiple frontend files | MEDIUM |
| 22 | main.tsx missing root element guard — white screen of death if #root absent | `main.tsx:6` | MEDIUM |

---

## Recommended Next Action

**Run `/crelyzor-review` again.** This codebase has 15 new CRITICAL issues and 25 new HIGH issues that were not present or not detected in the previous review pass. The previous review fixed what it targeted (21/23 confirmed), but the fresh scan surfaced a significant number of pre-existing issues that were not covered, plus two new regressions.

**Prioritized fix order for next review pass:**

1. **TD-N1** — Remove hardcoded personal URL from Toolbar.tsx (one-liner, deploy risk)
2. **SEC-N1** — Refresh token in OAuth redirect URL (security critical, long-lived token exposed)
3. **PERF-N1 / SCH-N5** — Card.isDeleted schema mismatch (data correctness, causes runtime errors)
4. **SF-N2** — Recording upload returns success when transcription queue failed (data loss)
5. **SF-N3** — AI parse failures stored as empty results permanently (silent data corruption)
6. **SF-N1** — Audio compression silent fallback (invisible ffmpeg misconfiguration)
7. **SF-N4** — ErrorBoundary no componentDidCatch (rendering crashes invisible in production)
8. **SCH-N1/N2/N3** — Schema relation gaps (MeetingNote.author, MeetingRecording.uploadedBy, fileSize Int)
9. **DB-N19/N20** — Session token rotation not atomic (partial failure locks users out)
10. **SEC-N2** — Inactive card accessible via slug on public endpoint
