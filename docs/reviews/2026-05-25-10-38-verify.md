# Crelyzor Verification Report — 2026-05-25 10:38
Verifying against: `docs/reviews/2026-05-25-10-19-review.md`

## Verdict: NOT READY — CRITICAL ISSUES REMAIN

6 new CRITICAL issues surfaced across the DB layer and booking management. All 31 previously reported fixes are confirmed. No regressions.

---

## Fixed Issues ✅ (31 of 31 confirmed)

| # | Issue | File | Confirmed By |
|---|-------|------|--------------|
| 1 | smaEditService updateMany + isDeleted:false at both sites | `smaEditService.ts:94,113` | DB layer, Security |
| 2 | deleteNote ownership via updateMany full scope | `aiController.ts:328` | Security, API contract, Silent failures |
| 3 | recallWebhookController isDeleted:false at both sites | `recallWebhookController.ts:316,358` | DB layer |
| 4 | taskController userId scope on all 5 update sites | `taskController.ts:524,586,595,616,624` | DB layer, Security |
| 5 | eventTypeService userId on update + updateMany soft-delete | `eventTypeService.ts:91,157` | DB layer |
| 6 | scheduleService findFirst with userId + isDeleted:false | `scheduleService.ts:203` | DB layer |
| 7 | tagService listTags take:500 | `tagService.ts:23` | DB layer, Performance |
| 8 | Prisma client regenerated (OAuthAccount.refreshToken Bytes?) | Prisma client | API contract |
| 9 | crelyzor-public port fallback 3000→4000 (3 files) | `cancel-form.tsx`, `cancel/page.tsx`, `CardView.tsx` | Security |
| 10 | transcriptController UUID validation on meetingId + segmentId | `transcriptController.ts:48,74` | API contract |
| 11 | aiController UUID validation on noteId | `aiController.ts:312` | API contract |
| 12 | aiController getNotes/createNote select clause (strips isDeleted) | `aiController.ts:171-218` | API contract |
| 13 | googleController apiResponse wrapping | `googleController.ts:78` | API contract |
| 14 | slotService take:200 on both findMany calls | `slotService.ts:183-199` | DB layer, Performance |
| 15 | googleCalendarPushService take:500 | `googleCalendarPushService.ts:252` | DB layer, Performance |
| 16 | adminService take:100 | `adminService.ts:43` | DB layer, Performance |
| 17 | googleCalendarPushService Promise.all parallelization | `googleCalendarPushService.ts:258-263` | Performance |
| 18 | meetingService removeExistingRecallDeployJobs Promise.all | `meetingService.ts:1107-1118` | Performance |
| 19 | userRoutes GET /search rate limit | `userRoutes.ts:9` | Security |
| 20 | useTagQueries invalidateQueries via queryKeys.tags.all | `useTagQueries.ts:98-119` | Frontend quality |
| 21 | useTagQueries toast.success on update/delete | `useTagQueries.ts:103,113` | Frontend quality |
| 22 | useTagItems staleTime: 5min | `useTagQueries.ts:23` | Frontend quality |
| 23 | TagDetailPage isError check + retry button | `TagDetailPage.tsx:27` | Frontend quality |
| 24 | CardContacts handleBulkTag Promise.allSettled + partial toast | `CardContacts.tsx:214` | Frontend quality, Silent failures |
| 25 | useCreateStandaloneTask narrowed to allTasks() | `useSMAQueries.ts:58` | Frontend quality |
| 26 | smaService SMATranscript.fullText optional | `smaService.ts:16` | API contract |
| 27 | TaskBoardView TaskStatus imported from @/types/meeting | `TaskBoardView.tsx:21` | Tech debt |

---

## Still Present ❌

None — all 31 previously reported fixes are confirmed.

---

## Regressions ⚠️

None detected.

---

## New Issues Found 🆕

### CRITICAL (6)

| # | File | Line | Issue | Recommendation |
|---|------|------|-------|----------------|
| N1 | `crelyzor-backend/src/services/speakerService.ts` | 18, 43 | `MeetingSpeaker.findMany` and `findFirst` missing `isDeleted: false` — soft-deleted speakers leak into transcript display | Add `isDeleted: false` to both where clauses |
| N2 | `crelyzor-backend/src/services/shareService.ts` | 163 | `MeetingSpeaker.findMany` missing `isDeleted: false` — public endpoint leaks deleted speakers | Add `isDeleted: false` |
| N3 | `crelyzor-backend/src/services/ai/aiService.ts` | 780 | `MeetingSpeaker.findMany` missing `isDeleted: false` and no `take` | Add both |
| N4 | `crelyzor-backend/src/services/ai/aiService.ts` | 83, 232 | `Meeting.findFirst` in `generateSummary`/`generateSummaryAndKeyPoints` missing `createdById: userId` ownership scope (TOCTOU) | Add `createdById: userId` to both |
| N5 | `crelyzor-backend/src/services/bookingManagementService.ts` | 166, 448, 569 | `tx.booking.update({ where: { id } })` without `userId` in where clause at 3 sites | Convert to `updateMany({ where: { id, userId } })` at all 3 |
| N6 | `crelyzor-backend/src/controllers/taskController.ts` | 717 | `tx.task.update({ where: { id: taskId } })` in `deleteTask` without `userId` scope | Add `userId` to where clause |

### HIGH (5)

| # | File | Line | Issue | Recommendation |
|---|------|------|-------|----------------|
| N7 | `crelyzor-backend/src/worker/jobProcessor.ts` | 759-762 | `GCAL_PUSH_SYNC` catch block returns `{ success: false }` instead of re-throwing — Bull marks job completed, never retries despite 3 configured attempts | Re-throw the error |
| N8 | `crelyzor-frontend/src/pages/card-contacts/CardContacts.tsx` | 191-204 | `handleBulkDelete` uses `deleteContact.mutate()` in forEach without `onError` — failures are invisible, success toast misfires | Add `onError` or switch to `mutateAsync` + `Promise.allSettled` |
| N9 | `crelyzor-frontend/src/pages/card-contacts/CardContacts.tsx` | 220-221 | `handleBulkTag` invalidates with hardcoded `['cards', 'contacts']` and `['tags', 'contact']` — stale after any queryKey rename | Replace with `queryKeys.*` helpers |
| N10 | `crelyzor-backend/src/controllers/aiController.ts` | 96, 141, 302, 321 | `apiResponse(...)` called without `return` at 4 sites — latent "headers already sent" crash if any code is added after | Add `return` |
| N11 | `crelyzor-backend/src/validators/meetingSchema.ts` | 77 | `limit` field has no `.max()` cap — client can send `limit=999999` triggering full-table scan | Add `.max(200)` |

### MEDIUM (5)

| # | File | Line | Issue | Recommendation |
|---|------|------|-------|----------------|
| N12 | `crelyzor-frontend/src/pages/tasks/Tasks.tsx` | 534-587 | `handleBulkComplete`, `handleBulkDelete`, `handleBulkPriority` use `Promise.all` — single failure aborts remaining ops; cache not invalidated on partial success | Switch to `Promise.allSettled` |
| N13 | `crelyzor-backend/src/services/meetings/meetingService.ts` | 612 | `updateMeeting` GCal catch has no logging — GCal failures on update are unobservable | Add `logger.warn` |
| N14 | `crelyzor-frontend/src/types/meeting.ts` | 83, 87-89 | Phantom frontend type fields `meetingProvider`, `guestEmail`, `guestName`, `guestMessage` — no corresponding Prisma columns, always undefined | Remove from Meeting type |
| N15 | `crelyzor-backend/src/controllers/aiController.ts` | 40-50 | `decrypt(summary.keyPoints)` catch returns `[]` with no logging — silent data loss | Add `logger.warn` |
| N16 | `crelyzor-backend/prisma/schema.prisma` | OAuthAccount, CardContact | Missing `updatedAt` on OAuthAccount (Phase 5 token rotation needs it); missing `createdAt`/`updatedAt` on CardContact (Phase 5 blind index writes need it) | Add before Phase 5 migration |

### LOW (8)

| # | File | Line | Issue | Recommendation |
|---|------|------|-------|----------------|
| N17 | `crelyzor-backend/src/worker/notificationSubscriber.ts` | 49 | `sharedSub.quit().catch(() => {})` swallows Redis quit error | Add `logger.warn` |
| N18 | `crelyzor-frontend/src/hooks/useNotificationSocket.ts` | 70-74 | WebSocket constructor failure silently stops reconnect loop | Add `console.warn` in dev |
| N19 | `crelyzor-frontend/src/pages/setup/SetupPage.tsx` | 11 | `VITE_CARDS_PUBLIC_URL` fallback is `https://cards.crelyzor.com` (prod URL) — should be `http://localhost:5174` | Fix fallback |
| N20 | `crelyzor-frontend/src/pages/pricing/PricingPage.tsx` | 19 | Same wrong `VITE_CARDS_PUBLIC_URL` fallback | Fix fallback |
| N21 | `crelyzor-frontend/src/pages/pricing/PricingPage.tsx` | 17 | Hardcoded personal email `harshkeshari100@gmail.com` | Move to env var or constants |
| N22 | `crelyzor-backend/src/utils/security/corsOptions.ts` | 10 | Stale `http://localhost:3000` in CORS allowlist — no browser in this project runs at 3000 | Remove |
| N23 | `crelyzor-frontend/src/pages/tasks/components/BulkActionBar.tsx` | 5 | `Priority` type declared locally — should be `TaskPriority` exported from `@/types/meeting` | Consolidate |
| N24 | `crelyzor-frontend/src/pages/tasks/components/CreateTaskModal.tsx` | 14 | Same local `Priority` re-declaration | Same fix |

---

## Recommended Next Action

Run `/crelyzor-review` to fix:

**CRITICAL priority (fix first):**
1. `speakerService.ts` — add `isDeleted: false` to MeetingSpeaker queries (N1)
2. `shareService.ts:163` — same (N2, public endpoint)
3. `aiService.ts:780` — same + add `take` (N3)
4. `aiService.ts:83,232` — add `createdById: userId` ownership (N4)
5. `bookingManagementService.ts:166,448,569` — convert 3 `booking.update` → `updateMany` with userId (N5)
6. `taskController.ts:717` — add userId to deleteTask tx.task.update (N6)

**HIGH priority:**
7. `jobProcessor.ts:759-762` — re-throw in GCAL_PUSH_SYNC catch (N7)
8. `CardContacts.tsx:191-204` — fix handleBulkDelete fire-and-forget (N8)
9. `CardContacts.tsx:220-221` — replace hardcoded query key arrays (N9)
10. `aiController.ts:96,141,302,321` — add return before apiResponse (N10)
11. `meetingSchema.ts:77` — add .max(200) to limit (N11)

---

```
CRELYZOR VERIFY COMPLETE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Verdict:        NOT READY — CRITICAL ISSUES REMAIN
Fixed:          31 of 31 confirmed (all previous fixes held)
Still present:  0
Regressions:    0
New issues:     24 (6 critical, 5 high, 5 medium, 8 low)
Report:         docs/reviews/2026-05-25-10-38-verify.md
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Run /crelyzor-review to fix remaining issues.
```
