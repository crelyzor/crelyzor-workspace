# Crelyzor Production Review — 2026-05-10

## Summary
- Total issues found: 46 (3 critical, 13 high, 18 medium, 12 low)
- Total issues fixed: 33
- Issues skipped: 13 (see below)
- Plan approval: Approved on iteration 3

## Issues Found & Fixed

### CRITICAL
| # | File | Issue | Status |
|---|------|-------|--------|
| 1 | `src/services/shareService.ts` | Public share page exposed tasks from any user — missing `userId` scope on task query; required adding `createdById` to meeting select | ✅ Fixed |
| 2 | `src/worker/jobProcessor.ts` (BOOKING_REMINDER) | Bull job body not wrapped in try-catch — unhandled exceptions kill job silently, no retry | ✅ Fixed |
| 3 | `src/worker/jobProcessor.ts` (DAILY_TASK_DIGEST) | Same silent failure pattern; also needed per-user isolation so one user's failure doesn't abort entire batch | ✅ Fixed |

### HIGH
| # | File | Issue | Status |
|---|------|-------|--------|
| 4 | `src/services/gcs/gcsService.ts` | All 5 GCS functions unguarded — raw SDK errors propagate, never mapped to AppError | ✅ Fixed |
| 5 | `src/services/scheduling/bookingService.ts` | `getPublicBooking` leaked `guestEmail` on public endpoint | ✅ Fixed |
| 6 | `src/validators/meetingSchema.ts` | Status filter used `z.enum([...])` string literals — drift risk if Prisma enum changes; switched to `z.nativeEnum(MeetingStatus)` | ✅ Fixed |
| 7 | `src/config/environment.ts`, `src/utils/security/corsOptions.ts`, `src/controllers/googleController.ts` | Multiple files reading `process.env.*` directly instead of validated `env` schema; CORS origins unvalidated | ✅ Fixed |
| 8 | `src/services/cardService.ts` | CSV export used `findMany` with `take: 10000` — OOM risk on large datasets; replaced with cursor-based batch loop (500/batch) | ✅ Fixed |
| 9a | `src/services/meetings/meetingService.ts` | `getMeetings` used full `meetingInclude` (loads all participant details) on list endpoint — O(N) over-fetch | ✅ Fixed |
| 9b | `src/services/meetings/meetingService.ts` | ICS import used per-event `findFirst` for dedup — race condition under concurrent imports; replaced with pre-batch UID dedup | ✅ Fixed |
| 9c | `src/services/meetings/meetingService.ts` | `getMeetings`/`getMeetingsWithoutPagination` had `as any[]` casts hiding type drift | ✅ Fixed |
| 10 | `src/services/cardService.ts` | `buildTemplateData(draftData as any, ...)` cast — unsafe; removed, added `MeetingWithDetails` type | ✅ Fixed |
| 11 | `src/services/scheduling/bookingManagementService.ts` | Host + guest confirmation emails in shared try-catch — one failure prevents both; split into independent blocks | ✅ Fixed |
| 12 | `src/hooks/queries/useSMAQueries.ts` | Polling already self-stops via `refetchInterval` callback — no raw `setInterval` found | ✅ Skip (already correct) |
| 13 | `src/services/meetings/meetingService.ts` | `createMeeting` returned opaque Meeting — no way for frontend to know if GCal sync succeeded | ✅ Fixed |
| 14 | `src/services/transcription/transcriptionService.ts` | `getTranscript` included full 150–300KB `fullText` field on every transcript fetch | ✅ Fixed |

### MEDIUM
| # | File | Issue | Status |
|---|------|-------|--------|
| 15 | `src/routes/smaRoutes.ts` | AI routes missing rate limiter | ✅ Skip (already had rate limiters — 20/hr ask, 10/hr generate, 5/hr regenerate) |
| 16 | `src/validators/cardSchema.ts` | Contact note field accepted raw HTML — XSS risk in stored notes | ✅ Fixed |
| 17 | `prisma/schema.prisma` | Missing indexes (AdminInvite, TranscriptSegment compound), MeetingNote.author non-FK nullable, MeetingRecording.uploadedBy missing FK | ✅ Fixed (schema + `prisma db push` applied) |
| 18 | `src/validators/bookingManagementSchema.ts` | Missing reschedule Zod validation | ✅ Skip (no reschedule endpoint exists yet — out of scope) |
| 19 | `src/lib/apiClient.ts` | `BillingLimitCode` set missing `FEATURE_GATE`; `code as any` cast | ✅ Fixed |
| 20 | `src/hooks/queries/useSettingsQueries.ts`, `useTagQueries.ts` | Missing `staleTime` on stable queries — unnecessary refetch on focus | ✅ Fixed |
| 21 | SMA backend services | `console.warn` calls instead of `logger.warn` | ✅ Skip (no console calls found in SMA services) |
| 22 | `src/validators/bookingManagementSchema.ts` | `page`/`limit` missing `z.coerce.number()` | ✅ Skip (already present at lines 28–29) |
| 23 | `src/services/scheduling/slotService.ts` | Sequential awaits for `override` + `availabilitySlots` — both independent of each other | ✅ Fixed |

### LOW
| # | File | Issue | Status |
|---|------|-------|--------|
| 24 | `src/utils/audioCompression.ts` | Dead file, never imported | ✅ Skip (already deleted) |
| 25 | `src/middleware/authMiddleware.ts` | `requireEmailVerified` and `getDeviceInfo` exported but never imported anywhere | ✅ Fixed |
| 26 | `src/validators/authSchema.ts` | Dead local-auth schemas (`loginSchema`, `registerSchema`, etc.) — Google OAuth replaced these | ✅ Fixed |
| 27 | `src/utils/errorFormatter.ts` | Dead file | ✅ Skip (already deleted) |
| 28 | `src/services/settingsService.ts` | Dead `availabilityApi` export (`Promise.resolve([])` stubs — never used) | ✅ Fixed |
| 29 | `src/hooks/queries/useCardQueries.ts` | `useDuplicateCard` and `useUpdateContactTags` never imported anywhere | ✅ Fixed |
| 30 | `SpeakerChip.tsx` | Exported but not imported from outside | ✅ Skip (file not found — already removed) |
| 31 | `src/lib/apiClient.ts` | `VITE_CARDS_PUBLIC_URL` fallback duplicated in 5 places | ✅ Skip (env var not found in frontend — moot) |
| 32 | 6 orphaned components | `CompactStickyBar`, `TodaysMeetings`, `QuickStatsRow`, `RecentVoiceNotes`, `MeetingsFilterBar`, `MeetingsTable` | ✅ Skip (already deleted) |
| 33 | `src/app/schedule/[username]/[slug]/page.tsx` | Missing `export const revalidate = 60` — event type profile refetched on every request | ✅ Fixed |

## Issues Skipped
| Issue | Reason |
|-------|--------|
| CHANGE 12 — useSMAQueries polling | Already self-stops via `refetchInterval: (query) => query.state.data ? false : 4000` |
| CHANGE 15 — AI route rate limiting | All AI routes already have `userRateLimit()` in smaRoutes.ts |
| CHANGE 18 — reschedule validation | No reschedule endpoint exists; adding validator alone would be dead code |
| CHANGE 21 — console.warn in SMA | No console calls found in SMA/AI/transcription services |
| CHANGE 22 — coerce on list schema | `z.coerce.number()` already applied to `page` and `limit` in listBookingsQuerySchema |
| CHANGE 24 — audioCompression.ts | File already deleted |
| CHANGE 27 — errorFormatter.ts | File already deleted |
| CHANGE 30 — SpeakerChip internalize | File not found; already removed |
| CHANGE 31 — VITE_CARDS_PUBLIC_URL | Env var not used anywhere in frontend codebase |
| CHANGE 32 — orphaned components | All 6 components already deleted |

## Files Changed

**crelyzor-backend:**
- `src/services/shareService.ts` — task userId scoping
- `src/worker/jobProcessor.ts` — try-catch wrapping for both Bull jobs + env centralization
- `src/services/gcs/gcsService.ts` — full AppError wrapping on all 5 functions
- `src/services/scheduling/bookingService.ts` — removed guestEmail from public select
- `src/validators/meetingSchema.ts` — z.nativeEnum(MeetingStatus) + import
- `src/config/environment.ts` — FRONTEND_URL, PUBLIC_URL, ALLOWED_ORIGINS fields
- `src/utils/security/corsOptions.ts` — env centralization
- `src/controllers/googleController.ts` — env centralization
- `src/services/scheduling/bookingManagementService.ts` — email isolation + env
- `src/services/cardService.ts` — cursor-based export + removed as any cast
- `src/services/meetings/meetingService.ts` — meetingListInclude, MeetingWithDetails, ICS dedup, gcalSynced return
- `src/controllers/meetingController.ts` — destructure gcalSynced from createMeeting
- `src/services/transcription/transcriptionService.ts` — select instead of include to omit fullText
- `src/validators/cardSchema.ts` — HTML strip on note field
- `prisma/schema.prisma` — AdminInvite indexes, MeetingNote FK + nullable, MeetingRecording FK, TranscriptSegment compound index
- `src/validators/authSchema.ts` — removed dead local-auth schemas
- `src/middleware/authMiddleware.ts` — removed requireEmailVerified, getDeviceInfo
- `src/services/scheduling/slotService.ts` — parallelize override + availability queries
- `src/services/settingsService.ts` (frontend copy) — N/A

**crelyzor-frontend:**
- `src/lib/apiClient.ts` — BillingLimitCode import + set + typed cast
- `src/hooks/queries/useSettingsQueries.ts` — staleTime: 5 min
- `src/hooks/queries/useTagQueries.ts` — staleTime on useTagsWithCounts
- `src/hooks/queries/useCardQueries.ts` — removed useDuplicateCard, useUpdateContactTags
- `src/services/settingsService.ts` — removed dead availabilityApi

**crelyzor-public:**
- `src/app/schedule/[username]/[slug]/page.tsx` — revalidate = 60

## Next Steps (manual actions required)
1. **Run migration in production** — `prisma db push` was applied to local dev DB. For staging/production, run `pnpm db:migrate` after ensuring `MeetingRecording.uploadedBy` has no orphaned user IDs:
   ```sql
   SELECT COUNT(*) FROM "MeetingRecording" r
   LEFT JOIN "User" u ON r."uploadedBy" = u.id
   WHERE u.id IS NULL;
   ```
   If count > 0, either fix the data or change MeetingRecording FK from Restrict to SetNull before migrating.

2. **Verify GCal sync status in frontend** — `createMeeting` now returns `{ meeting, gcalSynced }`. The frontend meeting creation flow should surface `gcalSynced: false` as a toast warning if GCal sync is expected but missed.

3. **CHANGE 18 (reschedule Zod validation)** — implement when the reschedule endpoint is built.
