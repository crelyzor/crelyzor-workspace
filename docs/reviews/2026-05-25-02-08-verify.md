# Crelyzor Verification Report — 2026-05-25 02:08
Verifying against: `2026-05-24-21-16-review.md`

## Verdict: NOT READY — CRITICAL ISSUES REMAIN

6 new critical issues found (previously missed due to partial agent coverage in the prior run). All 20 previously-fixed items are confirmed. 1 regression (partial fix). 23 new high issues.

---

## Fixed Issues ✅ — All 20 Confirmed

| # | File | Line | Severity | Confirmed By |
|---|------|------|----------|--------------|
| 1 | `cardService.ts` | 473 | CRITICAL | Security agent |
| 2 | `smaEditService.ts` | 82 | CRITICAL | DB layer + Performance agents |
| 3 | `userUpdateService.ts` | 54–63 | CRITICAL | Security agent |
| 4 | `auth/authService.ts` | 63–126 | CRITICAL | Security agent |
| 5 | `authController.ts` | 220, 252, 260 | CRITICAL | Security agent |
| 6 | `transcriptionService.ts` | 290 | CRITICAL | DB layer agent |
| 7 | `auth/sessionService.ts` | 50–79 | CRITICAL | DB layer agent |
| 8 | `auth/sessionService.ts` | 89–148 | CRITICAL | DB layer agent |
| 9 | `auth/sessionService.ts` | 208 | CRITICAL | DB layer agent (take:100 confirmed) |
| 10 | `adminService.ts` | 48–60 | CRITICAL | DB layer agent |
| 11 | `adminService.ts` | 63–100 | CRITICAL | DB layer agent |
| 12 | `ai/aiService.ts` | 757→764 | HIGH | Performance agent (take:5000 at line 764) |
| 13 | `smaEditService.ts` | 151→159 | HIGH | Performance agent (take:10000 at line 159) |
| 14 | `ai/askAIConversationService.ts` | 28→33 | HIGH | Performance agent (take:200 at line 33) |
| 15 | `billing/usageService.ts` | 293 | HIGH | Performance agent (count() confirmed) |
| 16 | `notificationService.ts` | 90–127 | HIGH | DB layer agent |
| 17 | `attachmentService.ts` | 253–263 | HIGH | DB layer agent |
| 18 | `scheduling/scheduleService.ts` | 85–112, 314–330 | HIGH | DB layer agent |
| 19 | `scheduling/bookingManagementService.ts` | 253–265 | HIGH | DB layer agent |
| 20 | `crelyzor-frontend/useSchedulingQueries.ts` | 24, 99, 199, 247, 291 | MEDIUM | Performance + Frontend agents (all 5 staleTime values confirmed) |

---

## Still Present (Deferred) ⏸️

| File / Area | Severity | Reason |
|-------------|----------|--------|
| Schema: composite indexes on Meeting, Task, CardContact, Booking, MeetingParticipant, MeetingShare | MEDIUM | Deferred post-Phase 5 migration |
| Schema: TranscriptSegment, AskAIMessage, AskAIConversation, OAuthAccount missing isDeleted/deletedAt | MEDIUM | Deferred Phase 5 concern |
| Schema: MeetingNote.author → authorId | LOW | Breaking rename, deferred |
| Schema: Card @@index([slug]) redundant | LOW | Low priority |
| aiController.ts:63-287 AI sync in HTTP request | CRITICAL | Architectural — needs Bull job types |
| worker/jobProcessor.ts:519 daily-digest OOM | CRITICAL | Architectural — needs pagination |
| worker/jobProcessor.ts:305 Recall Buffer OOM | CRITICAL | Architectural — needs streaming |
| meetings/meetingService.ts:907 getMeetings heavy include | HIGH | Deferred — architectural selector refactor |
| cardService.ts:1065 getCardMeetings heavy include | HIGH | Deferred — same as above |
| ai/aiService.ts:81,232,510 missing createdById scope | HIGH | Deferred — trusted-queue callers enforce upstream |
| meetings/meetingService.ts:438 detectConflicts unbounded | HIGH | Deferred — query consolidation risk |
| ai/aiService.ts Gemini char vs token cap | HIGH | Deferred — needs countTokens API |
| searchService.ts:40 ILIKE no trigram index | HIGH | Deferred — needs raw SQL migration |
| exportService.ts:18 PDF in-memory (sync) | HIGH | Deferred — needs Bull + signed URL |
| cardService.ts:776 CSV in-memory | HIGH | Deferred — needs Bull streaming |
| aiController.ts:63-146 no Redis SETNX rate limit on regenerate | HIGH | Deferred — needs Redis SETNX wrapper |

---

## Regressions ⚠️

| # | File | Line | Severity | Description |
|---|------|------|----------|-------------|
| 1 | `smaEditService.ts` | 94, 113 | CRITICAL | The fix for `meetingAISummary.findUnique` (issue #2) added `isDeleted:false` to the `findFirst` check but did NOT carry it into the `update()` calls inside the transaction (line 94) and on the non-transaction path (line 113). Both still call `prisma.meetingAISummary.update({ where: { meetingId } })` without `isDeleted:false`. A race between the check and the write can update a soft-deleted summary record. Fix: convert both to `updateMany({ where: { meetingId, isDeleted: false } })`. |

---

## New Issues Found 🆕

### CRITICAL (6 new)

| # | File | Line | Description | Fix Type |
|---|------|------|-------------|----------|
| 1 | `aiController.ts` | 328 | `deleteNote` uses `meetingNote.update({where:{id}})` with no ownership scope — TOCTOU race | FIXABLE |
| 2 | `recallWebhookController.ts` | 316, 358 | `meeting.update` by id only, no `isDeleted:false` (2 sites) — writes to soft-deleted meetings | FIXABLE |
| 3 | `taskController.ts` | 524, 586, 595, 616, 624 | `task.update({where:{id}})` with no `userId` scope (5 sites) — TOCTOU after ownership pre-check | FIXABLE |
| 4 | `scheduling/eventTypeService.ts` | 91, 157 | `eventType.update`/soft-delete by id only, no `userId` scope | FIXABLE |
| 5 | `tagService.ts` | 23 | `listTags` has no `take` — unbounded `findMany` returns all user tags in one query | FIXABLE |
| 6 | `SMATranscript.fullText` | `smaService.ts:18` / `transcriptionService.ts:340` | `fullText` declared as required `string` in frontend type but `getTranscript()` never selects it — always `undefined` at runtime | FIXABLE |

### HIGH (23 new)

| # | File | Line | Description | Fix Type |
|---|------|------|-------------|----------|
| 1 | `taskController.ts` | 524, 586, 595 | Task update + GCal writes not in `$transaction` — orphaned GCal events on partial failure | FIXABLE |
| 2 | `cardService.ts` | 455 | `card.update({where:{id:newCard.id}})` outside transaction for HTML write-back, missing `userId` scope | FIXABLE |
| 3 | `slotService.ts` | 183–199 | `booking.findMany` and `meeting.findMany` with no `take` in slot availability check | FIXABLE |
| 4 | `googleCalendarPushService.ts` | 252 | `gCalSyncState.findMany` no `take` in `renewExpiringChannels` | FIXABLE |
| 5 | `adminService.ts` | 43 | `adminUser.findMany` no `take` in `listAdmins` | FIXABLE |
| 6 | `googleCalendarPushService.ts` | 258–263 | GCal push renewal loop sequential per-user (not `Promise.all`) | FIXABLE |
| 7 | `meetingService.ts` | 1107–1118 | `removeExistingRecallDeployJobs` removes jobs one-at-a-time in loop, not `Promise.all` | FIXABLE |
| 8 | `meetingService.ts` | 1011–1077 | ICS import runs one `$transaction` per event sequentially — 200 events = 200 DB round trips | ARCHITECTURAL |
| 9 | `exportService.ts` | 100–147 | PDF generation synchronous in HTTP request path (blocks event loop) | ARCHITECTURAL |
| 10 | `cardService.ts` | 791 | `exportContacts` KMS N+1 — 3 KMS decrypt calls per contact sequentially | ARCHITECTURAL |
| 11 | `googleController.ts` | 78 | `POST /auth/google/calendar/connect` uses `res.json({url})`, bypasses `globalResponseHandler` | FIXABLE |
| 12 | `aiController.ts` | 312 | `DELETE /sma/notes/:noteId` missing UUID validation on `noteId` | FIXABLE |
| 13 | `transcriptController.ts` | 48, 74 | PATCH transcript/summary endpoints missing UUID validation on `meetingId`/`segmentId` | FIXABLE |
| 14 | `aiController.ts` | 171–218 | Notes CRUD returns full `MeetingNote` including `isDeleted`/`deletedAt` — no `select` clause | FIXABLE |
| 15 | `scheduleService.ts` | 203 | `availabilitySchedule.findUnique` missing `isDeleted:false` on post-transaction return | FIXABLE |
| 16 | `useTagQueries.ts` | 98–102, 114–119 | `useUpdateTag`/`useDeleteTag` invalidate hardcoded string arrays instead of `queryKeys` | FIXABLE |
| 17 | `useTagQueries.ts` | 103, 113 | `useUpdateTag`/`useDeleteTag` missing `toast.success` in `onSuccess` | FIXABLE |
| 18 | `TagDetailPage.tsx` | 27–76 | No `isError` check — network failure indistinguishable from "tag not found", no retry | FIXABLE |
| 19 | `CardContacts.tsx` | 214 | Bulk tag attach swallows per-contact errors, always shows success toast | FIXABLE |
| 20 | `useSMAQueries.ts` | 58 | `useCreateStandaloneTask` over-invalidates `queryKeys.sma.all` — busts all SMA queries | FIXABLE |
| 21 | Prisma client | `googleController.ts:224`, `googleService.ts:209` | Stale Prisma client — 2 tsc errors on `OAuthAccount.refreshToken` (schema has `Bytes?`, client has `Bytes`). Fix: `pnpm db:generate` | FIXABLE |
| 22 | `crelyzor-public` | `cancel-form.tsx:23`, `bookings/[id]/cancel/page.tsx:23`, `CardView.tsx:150` | Wrong port fallback (3000 instead of 4000) — booking cancellation and vCard download fail in local dev | FIXABLE |
| 23 | `taskController.ts` / `aiController.ts` | — | Task CRUD and Notes CRUD Prisma calls live directly in controller — violates Controller→Service→Prisma invariant | ARCHITECTURAL |

### MEDIUM (21 new)

| # | File | Line | Description |
|---|------|------|-------------|
| 1 | `tagService.ts` | 232 | `getTagItems` returns raw encrypted `Bytes` for `contact.email` — misleading, wasteful to serialize |
| 2 | `userRoutes.ts` / `userUpdateService.ts` | 9 / 23 | `GET /users/search` has no rate limit — authenticated user can enumerate all other users' emails |
| 3 | `bookingController.ts` | 65–67 | `getPublicBookingDetails` reads `req.query.username`/`slug` as raw strings without Zod schema |
| 4 | `bookingService.ts` | 489 | `createNotification` `.catch(()=>{})` with no logging — errors completely silenced |
| 5 | `meetingService.ts` | 338, 369 | GCal sync and Recall bot queue fail-open with empty catch — no log on failure |
| 6 | `ai/aiService.ts` | 551 | Decrypt/parse failure silently resets `keyPoints=[]` with no log — masks KMS/crypto errors |
| 7 | `worker/jobProcessor.ts` | 755–762 | GCal push sync job returns `{success:false}` on failure — Bull marks COMPLETED, invisible in dashboard |
| 8 | `useNotificationSocket.ts` | 72 | WebSocket construction failure silently exits hook with no log or user feedback |
| 9 | `crelyzor-public/error.tsx` | 14 | Empty `useEffect` body — no error reporting service wired, runtime errors on public pages invisible |
| 10 | `SetupPage.tsx` | 58–72 | Raw `useEffect`+`setTimeout` for username availability check — should be `useQuery` |
| 11 | `Home.tsx` | 38–43 | `meetingsError` destructured but never rendered — silent failure for home page meetings fetch |
| 12 | `useTagQueries.ts` | 23 | `useTagItems` missing `staleTime` — refetches on every navigation to tag detail |
| 13 | `cardService.ts` | 791 | Contacts export decrypts contacts sequentially within batch (not `Promise.all` across batch) |
| 14 | `VITE_CARDS_PUBLIC_URL` | `SetupPage.tsx:11`, `PricingPage.tsx:19` | 2 of 9 files use production URL as fallback instead of localhost — wrong in dev |
| 15 | `TaskBoardView.tsx` | 21 | `TaskStatus` re-declared locally as union type — must import from `types/meeting.ts:190` (CLAUDE.md violation) |
| 16 | Schema: `Meeting.deletedBy` | — | FK UUID stored without Prisma relation to User — referential integrity not enforced |
| 17 | Schema: `MeetingParticipant` | — | Missing `createdAt`, `updatedAt`, `isDeleted`, `deletedAt` — no soft-delete for participant removal |
| 18 | Schema: `MeetingAISummary` | — | Missing `createdAt` (`generatedAt` substitutes, not convention-compliant) |
| 19 | Schema: `MeetingAIContent`, `MeetingSpeaker`, `GCalSyncState`, `UserUsage`, `Subscription`, `AdminUser` | — | Missing `isDeleted`/`deletedAt` on 6 models that can be logically deleted |
| 20 | Schema: `Session`, `RefreshToken`, `OAuthAccount`, `TranscriptSegment`, `AskAIMessage`, `AdminInvite` | — | Missing `updatedAt` on 6 models that are mutated after creation |
| 21 | `cardService.ts:511` / `crelyzor-public/src/types/card.ts:42` | — | `PublicCardResponse.user` omits `id` but `CardUser` declares `id: string` as required — type drift |

### LOW (9 new)

| # | File | Line | Description |
|---|------|------|-------------|
| 1 | `SharedTabs.tsx` | 488 | `onEdit` callback not wrapped in `useCallback` — busts `memo` on all 500+ `SegmentRow` instances |
| 2 | `exportService.ts` | 1 | `pdfkit` imported at module load — loaded on every process start even without exports |
| 3 | `smaService.ts` | 462 | `console.warn` in production SSE error handler |
| 4 | `SetupPage.tsx` | 62 | Username availability check catch silently sets `null` — no user feedback |
| 5 | `useNotificationSocket.ts` | 96 | Message parse failure not logged |
| 6 | `Settings.tsx` | 2240, 2262 | Suppressed `react-hooks/exhaustive-deps` — potential stale closures |
| 7 | `CreateTaskModal.tsx` | 106 | Suppressed `react-hooks/exhaustive-deps` — potential stale closure |
| 8 | Schema: `Notification.entityId` | — | Missing `@@index([entityId])` — lookups by entity have no index |
| 9 | Schema: `LoginHistory`, `CardView` | — | Missing schema comments explaining why audit/analytics rows omit soft-delete fields |

---

## Recommended Next Action

**Run `/crelyzor-review` again to fix remaining issues.**

Priority order for the next fix pass:

### Immediate — Fix Now (all FIXABLE, high blast radius)

1. **`smaEditService.ts:94,113`** (regression) — Convert both `meetingAISummary.update` calls to `updateMany({ where: { meetingId, isDeleted: false } })`
2. **`taskController.ts`** — Add `userId` scope to all 5 `task.update` calls (lines 524, 586, 595, 616, 624)
3. **`aiController.ts:328`** — Convert `deleteNote` `meetingNote.update` to scoped `updateMany`
4. **`recallWebhookController.ts:316,358`** — Add `isDeleted:false` to both `meeting.update` calls → `updateMany`
5. **`eventTypeService.ts:91,157`** — Add `userId` scope to both `eventType.update`/`delete`
6. **`scheduleService.ts:203`** — Convert `findUnique` → `findFirst` with `isDeleted:false`
7. **`tagService.ts:23`** — Add `take: 500` to `listTags` `findMany`
8. **`SMATranscript.fullText`** — Mark as `fullText?: string` in `smaService.ts:18` or add to `getTranscript()` select and decrypt
9. **`pnpm db:generate`** — Regenerate Prisma client to fix 2 tsc errors (OAuthAccount.refreshToken nullable)
10. **`crelyzor-public` port 3000→4000** — Fix in `cancel-form.tsx`, `bookings/[id]/cancel/page.tsx`, `CardView.tsx`

### High Priority — Fix This Sprint

11. `aiController.ts:312` — Add UUID validation on `noteId`
12. `transcriptController.ts:48,74` — Add UUID validation on `meetingId`/`segmentId`
13. `aiController.ts:171-218` — Add `select` clause to notes CRUD (exclude `isDeleted`/`deletedAt`)
14. `googleController.ts:78` — Replace `res.json({url})` with `globalResponseHandler`
15. `CardContacts.tsx:214` — Track per-contact failures in bulk tag attach; show partial-success toast
16. `useTagQueries.ts:98-119` — Use `queryKeys.tags.all` for invalidation; add `toast.success`
17. `TagDetailPage.tsx:27` — Add `isError` state + retry button
18. `useSMAQueries.ts:58` — Narrow invalidation from `queryKeys.sma.all` to `queryKeys.sma.allTasks()`
19. `slotService.ts:183-199` — Add `take: 200` to both `findMany` calls
20. `googleCalendarPushService.ts:252,258` — Add `take: 500`; parallelize renewal with `Promise.all`
21. `meetingService.ts:1107` — Parallelize job removal with `Promise.all`
22. `userRoutes.ts:9` — Add rate limit to `GET /users/search`
23. `tagService.ts:232` — Remove `email` from `getTagItems` contact select
24. `TaskBoardView.tsx:21` — Import `TaskStatus` from `types/meeting.ts`

### Architectural — Schedule Separately

25. ICS import batching (meetingService.ts:1011)
26. PDF export to Bull queue (exportService.ts)
27. Export contacts KMS N+1 → batch DEK reuse (cardService.ts:791)
28. Task CRUD → taskService.ts layer extraction
29. Schema soft-delete and timestamp gaps across 11+ models
