# Crelyzor Verification Report — 2026-03-25-11-45
Verifying against: `2026-03-25-10-27-review.md`

## Verdict: NOT READY — CRITICAL ISSUES REMAIN

**10 of 22 previously-fixed issues confirmed. 13 still present. 25 new CRITICAL issues found.**

The previous fix pass addressed surface-level symptoms without applying fixes systematically across the codebase. The same patterns (missing `isDeleted: false` on `MeetingTranscript`, `renderCardHtml` inside transactions, missing UUID validation) were fixed in the specific files called out but left broken in 10–15 other identical call sites.

---

## Fixed Issues ✅

| Issue | File | Confirmed By |
|-------|------|--------------|
| Missing `TooManyRequestsError` (429) class | globalErrorHandler.ts | Security agent: rate limiting PASS |
| Missing rate limiter on `/auth/refresh-token` | authRoutes.ts | Security agent: auth routes PASS |
| Multi-step DB ops without `$transaction` in sessionService | sessionService.ts | DB agent: sessionService CLEAN |
| `deactivateAccount` transaction missing `{ timeout: 15000 }` | authService.ts | DB agent: authService CLEAN |
| No cap on `getAttachments`; no `signedUrlError`; unguarded `new URL()` | attachmentService.ts | DB agent: attachmentService CLEAN; Security agent: PASS |
| `getToken()` and `oauth2.userinfo.get()` not wrapped in try/catch | googleService.ts | DB agent: googleService CLEAN |
| Missing UUID validation on meetingId and taskId params | taskController.ts | Security agent: taskController PASS |
| Hardcoded personal booking URL | Toolbar.tsx | Tech debt: not re-flagged; env-backed constant confirmed |
| `console.error` in production token refresh path | apiClient.ts | Tech debt: now DEV-only guarded |
| NoteSchema extracted from inline dynamic import | noteSchema.ts | Not re-flagged by any agent |

---

## Still Present ❌

| Issue | File | Severity | Original Report Reference |
|-------|------|----------|--------------------------|
| Missing `isDeleted: false` on MeetingTranscript in `buildTranscriptText` (transcript-level, not just recording-level) | exportService.ts ~L14 | CRITICAL | Issue #2 |
| Missing `isDeleted: false` on MeetingAISummary in `buildSummaryText` | exportService.ts ~L32 | CRITICAL | Issue #2 |
| Missing `isDeleted: false` on transcript in `processTranscriptWithAI`, `generateContent`, `askAI` | aiService.ts ~L315, L401, L517 | CRITICAL | Issue #3 |
| Empty OpenAI content guards missing in `extractKeyPoints`, `extractTasks` (return `[]` silently on parse fail); `generateContent` saves empty string | aiService.ts | HIGH | Issue #4 |
| Missing `isDeleted: false` on transcript in `regenerateSummary`, `regenerateTitle` | aiController.ts ~L61, L103 | CRITICAL | Issue #5 |
| Missing `isDeleted: false` on transcript in `triggerAIProcessing` | recordingService.ts ~L283 | CRITICAL | Issue #6 |
| `GET /sma/meetings/:id/recordings` hardcoded `take: 20` with no pagination contract | recordingService.ts ~L185 | CRITICAL | Issue #6 |
| `globalErrorHandler` called directly from middleware instead of `next(error)` | authMiddleware.ts L68 | HIGH | Issue #8 |
| `renderCardHtml` inside `$transaction` — updateCard fixed but `duplicateCard` has same bug | cardService.ts ~L419 | CRITICAL | Issue #14 |
| `include: { participants: true }` over-fetch still present in `getMeetings` / `getMeetingsWithoutPagination` via `meetingInclude` | meetingService.ts L464, L508 | HIGH | Issue #15 |
| `as any` casts still present on `findMany` returns | meetingService.ts L470, L513, L522 | MEDIUM | Issue #15 |
| UUID validation missing across aiController, recordingController, transcriptController, speakerController, cardController (partial fix — only taskController fixed) | Multiple controllers | LOW | Issue #17 |
| `useTagQueries.ts` — overcorrected from under-invalidation to over-invalidation of entire `tags.all` tree | useTagQueries.ts | HIGH | Issue #19 — REGRESSION |

---

## Regressions ⚠️

| Issue | File | Severity | What It Broke |
|-------|------|----------|---------------|
| `useUpdateTag`/`useDeleteTag` invalidation overcorrected — now blows away entire `['tags']` namespace including all `byMeeting`/`byCard` caches on every tag update | useTagQueries.ts | HIGH | Was under-invalidating `userTags` only; now over-invalidating everything — cascade of unnecessary refetches on every tag rename |
| `ErrorBoundary.tsx` — `console.error` removed from production path but replaced with nothing; production crashes now completely invisible | ErrorBoundary.tsx | HIGH | Previous fix was console.error in production → now DEV-only; but no production monitoring added to replace it |

---

## New Issues Found 🆕

### CRITICAL

| Issue | File | Recommendation |
|-------|------|----------------|
| `createMeeting` / `updateMeeting` final `findUnique` missing `isDeleted: false` | meetingService.ts ~L156, L316 | Add `isDeleted: false` + ownership scope to both return queries |
| `getMeetingTags` findMany on `MeetingSpeaker` with no `isDeleted: false` | speakerService.ts ~L18 | Add `isDeleted: false` to where clause |
| `getPublicMeetingByShortId` — findMany on `MeetingSpeaker`, findFirst on `MeetingTranscript` and `MeetingAISummary` all missing `isDeleted: false` | shareService.ts ~L154, L166, L184 | Add `isDeleted: false` to all three queries |
| `getTranscript` — `MeetingTranscript` findFirst + segments missing `isDeleted: false` | transcriptionService.ts ~L296 | Add to both transcript and segments where clause |
| `include: { meeting: true }` fetches entire Meeting row just to check two fields | transcriptionService.ts L42 | Replace with `select: { id: true, meetingId: true, gcsPath: true, isDeleted: true, meeting: { select: { isDeleted: true, createdById: true } } }` |
| `getContacts` findMany on `CardContact` missing `isDeleted: false` | cardService.ts ~L602 | Add `isDeleted: false` — prerequisite for CardContact soft-delete migration |
| Schema: `Task.user`, `Card.user`, `CardContact.user` all use `onDelete: Cascade` — hard-deleting a user silently destroys all tasks and cards | schema.prisma | Change to `onDelete: Restrict` on all three; requires migration |
| Multer file-type and file-size rejections surface as 500 instead of 400/413 | uploadMiddleware.ts, attachmentUploadMiddleware.ts | Intercept `err.code === 'LIMIT_FILE_SIZE'` and rethrow as `AppError('File too large', 413)` |
| `handleCopyPublicLink` / `handleDisableLink` — empty catch blocks swallow all errors | ShareSheet.tsx L149, L159 | Either remove catch or add `toast.error('Failed to update share settings')` |
| `generateMeetingTitle` added to `Promise.all` with result discarded — not fire-and-forget; unexpected throw kills entire AI pipeline | aiService.ts L323-328 | Move to `void generateMeetingTitle(...).catch(err => logger.error(...))` outside Promise.all |
| `getTokenExpiration` silently returns `null` with no log trail | tokenService.ts L126-136 | Add `logger.warn` on unexpected null path |
| `AuthGuard.tsx` — `isError` never handled; profile fetch failure silently renders authenticated app with no data | AuthGuard.tsx | Add `isError` handling: redirect to `/signin` or show error fallback |
| `GET /meetings` pagination response missing `total` field — `count` is page size, not total records | meetingController.ts, meetingService.ts | Add `prisma.meeting.count({ where })` in parallel and return `{ count, total, limit, offset }` |
| `GET /meetings/:meetingId` — `guests` relation never included despite frontend `Meeting` type requiring it | meetingController.ts ~L183 | Add `guests: true` to include block; move query into meetingService |
| `GET /sma/meetings/:id/recordings` — `take: 20` silently truncates with no pagination response contract | recordingController | Expose `limit`/`offset` params; return `{ recordings, pagination: { total, limit, offset } }` |
| `GET /sma/meetings/:id/generated` — no cap on generated content list | aiController | Add `take: 50` defensive cap |

### HIGH

| Issue | File | Recommendation |
|-------|------|----------------|
| N+1 on participant conflict detection (N round-trips for N participants) | meetingService.ts ~L227 | Batch all participant IDs into single Prisma query |
| `askAI` — `MeetingTranscript` findFirst missing `isDeleted: false` | aiService.ts ~L401 | Add `isDeleted: false` to transcript and segments where clause |
| Status update to `PROCESSING` outside transaction — stuck if crash between update and transcript creation | transcriptionService.ts ~L50 | Move PROCESSING status write inside the transaction or document the retry guard |
| `deleteRecording` transaction wraps single write; linked `MeetingTranscript` not soft-deleted (orphan) | recordingService.ts ~L254 | Add soft-delete of linked transcript inside same transaction |
| Concurrent upsert race between `generateSummary` and `extractKeyPoints` — can overwrite summary with empty string | aiController.ts ~L71 | Sequence the two calls or merge into single upsert after both OpenAI calls complete |
| `getTasks` — no `take` limit (unbounded findMany) | taskController.ts ~L28 | Add `take: 500` |
| `exportContacts` — `take: 10000` loads potentially 10k rows into Node memory | cardService.ts ~L663 | Stream or cursor-paginate |
| `viewsByDay` — loads all `CardView` rows into memory for JS day-grouping | cardService.ts ~L751 | Replace with `$queryRaw` using `DATE_TRUNC` |
| `createOrGetShare` — race between findUnique and create; P2002 reaches global handler as 500 | shareService.ts ~L22 | Wrap in `$transaction` or catch P2002 and return existing record |
| `completeMeeting` — ownership checked at application level after DB fetch instead of in WHERE clause | meetingService.ts ~L376 | Scope initial query to `requesterUserId` via `OR [createdById, participants.some(ORGANIZER)]` |
| `user.id` (internal UUID) returned to unauthenticated public callers in `getPublicCard` | cardService.ts ~L482 | Remove `id` from the `user` object in public response |
| `extractKeyPoints`/`extractTasks` return `[]` on JSON parse failure with no status update or user feedback | aiService.ts L129-134, L211-216 | Propagate error or persist `AI_PARTIAL` status so UI can surface retry option |
| `generateContent` saves empty string on OpenAI empty response (no guard present unlike `generateSummary`) | aiService.ts L545 | Add same empty-content guard as `generateSummary`: throw `AppError` on empty content |
| Queue init failure logged at `warn`; server starts broken with null queue references | index.ts L31-43 | Log at `logger.error` level; add health-check endpoint reporting queue status |
| `globalErrorHandler` called directly from middleware; Express error chain bypassed | authMiddleware.ts L68, L143, L246 | Replace direct calls with `next(error)`; register handler as `app.use((err, req, res, next) => ...)` |
| `trackView` failure causes public card endpoint to 500 instead of serving card gracefully | cardController.ts L313-319 | Wrap `trackView` in its own try/catch; log at warn and continue |
| `autoRefreshToken` catch is too broad — swallows programming bugs at warn level | authMiddleware.ts L96-110 | Narrow to JWT-specific errors; log unexpected errors at error level |
| `app.ts` global error handler exposes `err.message` in production (no `NODE_ENV` guard) | app.ts L46-49 | Use same production guard as `globalErrorHandler.ts` or delegate to it |
| Delete Account button has no `onClick` handler — dead UI element | Settings.tsx L424-430 | Wire to `authService.deactivateAccount` with confirmation dialog |
| Ask AI non-200 response discards HTTP status and body; generic message only | smaService.ts L294-297 | Read response JSON body; surface server `message` field; special-case 429 |
| All status mutations in `useMeetingQueries.ts` invalidate entire `meetings.all` cache instead of targeted keys | useMeetingQueries.ts | Invalidate `meetings.detail(id)` + `meetings.list()` only; use `removeQueries` on delete |
| `CardAnalytics.tsx` — loading and error branches missing `<PageMotion>` | CardAnalytics.tsx | Wrap both branches in `<PageMotion>` |
| `CardContacts.tsx` — error state has no retry/refetch action | CardContacts.tsx | Surface `refetch` from `useCardContacts`; render "Try again" button |
| `Home.tsx` — missing `<PageMotion>` wrapper (only page without it) | Home.tsx | Wrap return in `<PageMotion>` |
| `findFirst` ownership checks load `htmlContent`/`htmlBackContent` (large generated HTML) unnecessarily | cardService.ts L236, L344, L378, L693 | Add explicit `select` excluding htmlContent/htmlBackContent on ownership-check queries |
| `getPublicMeetingByShortId` re-traverses junction chains after already resolving `meetingId` | shareService.ts L154-207 | Use resolved `meetingId` directly in subsequent parallel queries |
| `getMeetings` / `getMeetingsWithoutPagination` use full `meetingInclude` (loads all participants + users for list) | meetingService.ts L464, L508 | Define lean `meetingListInclude` for list endpoints |
| Schema: `User.deletedById`, `Meeting.deletedBy`, `MeetingRecording.uploadedBy` — `@db.Uuid` fields with no Prisma relation and no `@@index` | schema.prisma | Wire relations or add `@@index` for each |
| Schema: `MeetingNote.author` — should be `userId` with proper `User` relation | schema.prisma | Rename column (migration with explicit `RENAME COLUMN`); add `User` relation |
| `getMeetingsSchema` status filter — only 3 of 7 `MeetingStatus` values accepted; other 4 return 400 | meetingSchema.ts L62 | Expand z.enum to all 7 status values |
| `shareController.ts` async handlers without try/catch | shareController.ts | Verify Express 5 in use; if Express 4, add try/catch or asyncHandler wrapper |
| `ErrorBoundary.tsx` production crashes completely invisible — no monitoring integration | ErrorBoundary.tsx | Add production error reporting in `componentDidCatch` |

### MEDIUM

| Issue | File | Recommendation |
|-------|------|----------------|
| `renderCardHtml` inside `$transaction` in `duplicateCard` (same bug fixed in updateCard) | cardService.ts ~L419 | Hoist renderCardHtml out of transaction — same pattern as createCard |
| Schema: missing `isDeleted`/`deletedAt` on Session, LoginHistory, RefreshToken, OAuthAccount, MeetingParticipant, MeetingStateHistory, MeetingTranscript, TranscriptSegment, MeetingSpeaker, MeetingAISummary, MeetingAIContent, CardContact, CardView, MeetingTag, CardTag | schema.prisma | Add soft-delete fields in migration |
| Schema: missing `createdAt`/`updatedAt` on Session, LoginHistory, RefreshToken, OAuthAccount, MeetingParticipant, MeetingStateHistory, MeetingRecording, TranscriptSegment, MeetingAISummary, CardContact, CardView | schema.prisma | Add timestamp fields in migration |
| `deleteFile` ownership check uses path-segment string scan instead of DB-level record | storage.controller.ts ~L181 | Add `storage_files` DB table with `userId` FK |
| Raw error re-thrown from queue failure instead of `AppError(503)` | recordingService.ts L305-311 | Convert to `new AppError("AI processing could not be queued", 503)` |
| `revokeSession` revokes ALL refresh tokens for user, not just current session | sessionService.ts L122-137 | Add `sessionId` filter to `refreshToken.updateMany` |
| Empty catch in ffprobe strategy loop; no debug logging | getAudioDuration.ts L43-44 | Add `logger.debug` with strategy number and error message |
| Global `queryClient` mutation `onError` double-fires toast with per-mutation handlers | queryClient.ts L14-17 | Remove global mutation `onError`; rely on per-mutation error handlers |
| OAuth failure without `?error=` param gives no diagnostic info | AuthCallback.tsx L31-38 | Log failed OAuth state and include context in error message |
| `useLogout` has no `onError` handler | useAuthQueries.ts L40-51 | Add onError toast before session is cleared |
| `console.warn` for SSE parse errors without DEV guard (fires in production) | smaService.ts L334-339 | Wrap in `import.meta.env.DEV` guard |
| Original Google OAuth error details discarded before rethrowing | googleService.ts L35-39 | Add `logger.error` with original error before throwing AppError |
| `speakerNames` object recreated on every render — defeats React.memo on all N segment rows | RecordedDetail.tsx L166-169 | Wrap in `useMemo([speakers])` |
| Shared `isPending` boolean causes all N segment rows to re-render on any single save | SharedTabs.tsx | Track `pendingSegmentId` in state instead of sharing boolean |
| `getContacts` findMany loads all fields including note and tags array (no select) | cardService.ts L602 | Add explicit select to list query |
| `ScheduledDetail.tsx` — ErrorBoundary instances have no fallback prop (full-screen overlay inside tab panels) | ScheduledDetail.tsx | Add inline fallback prop to all 8 ErrorBoundary instances |
| SMA response nesting inconsistency — two unwrap strategies in smaService.ts | Multiple SMA controllers | Standardize all SMA endpoints to nest under key (`data: { task }`, `data: { tasks }`, etc.) |
| `Meeting` type drift — `guests`, `recording`, `aiSummary`, `tasks` fields expected by frontend never returned in meeting GET endpoints | meetingController.ts, meetingService.ts | Document as intentional separate-fetch pattern or include in response |
| `SMARecording.uploadedAt` typed as `Date` in backend, `string` in frontend | recordingService.ts | Change `RecordingResponse.uploadedAt` to `string`; serialize as `.toISOString()` |
| `POST /sma/meetings/:id/share` — wrong HTTP method for idempotent read-or-create | shareController.ts, smaRoutes.ts | Add `GET` route for read-or-create; rename POST to strict create-only |

---

## Recommended Next Action

**Run `/crelyzor-review` again.** This codebase has accumulated systemic gaps that require a full re-pass, not targeted fixes.

**Priority order for the next fix pass:**

1. **`isDeleted: false` on `MeetingTranscript`** — Apply this fix to ALL 14 identified call sites across `aiService.ts`, `transcriptionService.ts`, `recordingService.ts`, `exportService.ts`, `shareService.ts`, `aiController.ts`. This is a single-pattern fix that must be applied codebase-wide.

2. **Schema `onDelete: Cascade` on `Task`, `Card`, `CardContact`** — Hard-delete risk. Change to `Restrict` and run migration before any production traffic.

3. **Pagination `total` field** — `GET /meetings` is broken for any UI that needs to know total pages. Fix in `meetingService.getMeetings`.

4. **`guests` missing from `GET /meetings/:id`** — Frontend type declares it required; it's always absent; any component iterating `meeting.guests` will crash silently.

5. **`processTranscriptWithAI` Promise.all title generation** — Can silently kill the entire AI pipeline.

6. **`AuthGuard.tsx` isError** — Users with failed profile fetches land in a silent broken state.

7. **`useTagQueries.ts` regression** — Over-invalidation regression from the previous fix.

8. **`meetingSchema.ts` status filter** — 4 valid status values currently return 400; blocks any filtering UI.
