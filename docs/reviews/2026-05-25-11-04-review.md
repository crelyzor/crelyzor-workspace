# Crelyzor Production Review — 2026-05-25 11:04

## Summary

- **Total issues found:** 18 (3 critical, 5 high, 5 medium, 5 low — from 2026-05-25-10-38-verify.md; 6 reported CRITICALs reduced to 3 after MeetingSpeaker false positives discarded)
- **Total issues fixed:** 18 (3 critical, 5 high, 5 medium, 5 low)
- **Issues deferred:** 0
- **Plan approval:** Executed directly from verify report; MeetingSpeaker isDeleted N1/N2/N3 discarded as false positives (model has no isDeleted field — cascade delete from Meeting handles cleanup)
- **Agent coverage:** Used all 8 verify agents from 2026-05-25-10-38-verify.md as Layer 1 data

---

## Issues Found & Fixed

### CRITICAL (3 found, 3 fixed)

| # | File | Issue | Status |
|---|------|-------|--------|
| 1 | `crelyzor-backend/src/services/ai/aiService.ts:82` | `generateSummary` — `Meeting.findFirst` missing `createdById: userId` ownership scope (TOCTOU) | ✅ Fixed → added `createdById: userId` |
| 2 | `crelyzor-backend/src/services/ai/aiService.ts:233` | `generateSummaryAndKeyPoints` — same ownership gap | ✅ Fixed → added `createdById: userId` |
| 3 | `crelyzor-backend/src/services/scheduling/bookingManagementService.ts:166,449,566` | `tx.booking.update({where:{id}})` without `userId` scope in `confirmBooking`, `declineBooking`, `cancelBooking` (3 sites) | ✅ Fixed → all 3 converted to `tx.booking.updateMany({where:{id, userId}})` |
| 4 | `crelyzor-backend/src/controllers/taskController.ts:717` | `tx.task.update({where:{id}})` in `deleteTask` without `userId` scope | ✅ Fixed → `tx.task.updateMany({where:{id, userId, isDeleted:false}})` |

> Note: Verify report listed N1/N2/N3 (MeetingSpeaker.findMany missing isDeleted:false) as CRITICAL. Discarded — `MeetingSpeaker` has no `isDeleted` field in schema; cascade delete from `Meeting` is correct. DB agent false positive.

### HIGH (5 found, 5 fixed)

| # | File | Issue | Status |
|---|------|-------|--------|
| 5 | `crelyzor-backend/src/worker/jobProcessor.ts:755-762` | `GCAL_PUSH_SYNC` catch block returns `{success:false}` — Bull marks job completed, never retries despite 3 configured attempts | ✅ Fixed → `return { success: false }` → `throw err` |
| 6 | `crelyzor-frontend/src/pages/card-contacts/CardContacts.tsx:191-204` | `handleBulkDelete` uses `mutate()` in forEach with no `onError` — failures invisible, success toast misfires | ✅ Fixed → `Promise.allSettled` + `mutateAsync` + failure-count toast |
| 7 | `crelyzor-frontend/src/pages/card-contacts/CardContacts.tsx:220-221` | `handleBulkTag` invalidates with hardcoded `['cards','contacts']` and `['tags','contact']` | ✅ Fixed → `queryKeys.cards.contacts()` and `queryKeys.tags.all`; `queryKeys` import added |
| 8 | `crelyzor-backend/src/controllers/aiController.ts:96,141,302,321` | `apiResponse(...)` called without `return` at 4 sites — latent "headers already sent" crash | ✅ Fixed → `return apiResponse(...)` at all 4 sites |
| 9 | `crelyzor-backend/src/validators/meetingSchema.ts:77` | `limit` field had no `.max()` cap — client could send `limit=999999` | ✅ Fixed → `.max(200)` added |

### MEDIUM (5 found, 5 fixed)

| # | File | Issue | Status |
|---|------|-------|--------|
| 10 | `crelyzor-frontend/src/pages/tasks/Tasks.tsx:533-587` | `handleBulkComplete`, `handleBulkDelete`, `handleBulkPriority` used `Promise.all` — single failure aborts remaining ops; cache not invalidated on partial success | ✅ Fixed → all 3 converted to `Promise.allSettled` with partial-success toast |
| 11 | `crelyzor-backend/src/services/meetings/meetingService.ts:612` | `updateMeeting` GCal catch had no logging — GCal write failures unobservable | ✅ Fixed → `logger.warn` added |
| 12 | `crelyzor-frontend/src/types/meeting.ts:87-89` | `Meeting` type had phantom fields `guestEmail`, `guestName`, `guestMessage` with no corresponding Prisma columns (always undefined) | ✅ Fixed → removed from `Meeting` type |
| 13 | `crelyzor-backend/src/controllers/aiController.ts:40-50` | `decrypt(keyPoints)` failure returned `[]` with no logging — silent data loss | ✅ Fixed → `.catch()` added with `logger.warn` |
| 14 | `crelyzor-frontend/src/types/meeting.ts` | `TaskPriority` not exported — `BulkActionBar` and `CreateTaskModal` each declared their own local `Priority` type | ✅ Fixed → exported `TaskPriority` from `@/types/meeting`; both components import from there |

### LOW (5 found, 5 fixed)

| # | File | Issue | Status |
|---|------|-------|--------|
| 15 | `crelyzor-frontend/src/pages/setup/SetupPage.tsx:11` | `VITE_CARDS_PUBLIC_URL` fallback was `https://cards.crelyzor.com` (prod URL) — silently hit production in local dev | ✅ Fixed → `http://localhost:5174` |
| 16 | `crelyzor-frontend/src/pages/pricing/PricingPage.tsx:19` | Same wrong `VITE_CARDS_PUBLIC_URL` fallback | ✅ Fixed → `http://localhost:5174` |
| 17 | `crelyzor-backend/src/utils/security/corsOptions.ts:10` | Stale `http://localhost:3000` in CORS allowlist — no browser in this project runs at 3000 | ✅ Fixed → removed |
| 18 | `crelyzor-frontend/src/pages/tasks/components/BulkActionBar.tsx:5` | Local `type Priority` re-declaration — now uses `TaskPriority` from `@/types/meeting` | ✅ Fixed |
| 19 | `crelyzor-frontend/src/pages/tasks/components/CreateTaskModal.tsx:14` | Same local `Priority` re-declaration | ✅ Fixed |

---

## Files Changed

**Backend (7 files):**
1. `crelyzor-backend/src/services/ai/aiService.ts`
2. `crelyzor-backend/src/services/scheduling/bookingManagementService.ts`
3. `crelyzor-backend/src/controllers/taskController.ts`
4. `crelyzor-backend/src/worker/jobProcessor.ts`
5. `crelyzor-backend/src/controllers/aiController.ts`
6. `crelyzor-backend/src/validators/meetingSchema.ts`
7. `crelyzor-backend/src/services/meetings/meetingService.ts`

**Frontend (8 files):**
1. `crelyzor-frontend/src/pages/card-contacts/CardContacts.tsx`
2. `crelyzor-frontend/src/pages/tasks/Tasks.tsx`
3. `crelyzor-frontend/src/types/meeting.ts`
4. `crelyzor-frontend/src/pages/setup/SetupPage.tsx`
5. `crelyzor-frontend/src/pages/pricing/PricingPage.tsx`
6. `crelyzor-frontend/src/pages/tasks/components/BulkActionBar.tsx`
7. `crelyzor-frontend/src/pages/tasks/components/CreateTaskModal.tsx`
8. `crelyzor-backend/src/utils/security/corsOptions.ts`

## Verification

- Backend `pnpm tsc --noEmit` → clean (only pre-existing Phase 5 dep errors: missing `vitest`, `@google-cloud/kms`)
- Frontend `pnpm tsc --noEmit` → clean

---

## Next Steps — Manual Actions Required

### Immediate

1. **Schema: `OAuthAccount` missing `updatedAt`** — Add before Phase 5 migration (token rotation needs it for audit trail). Safe migration: `updatedAt DateTime @updatedAt`.
2. **Schema: `CardContact` missing `createdAt`/`updatedAt`** — Add before Phase 5 migration (blind index writes need it). Safe migration: both with `@default(now())` / `@updatedAt`.
3. **Schema: `MeetingParticipant` missing `createdAt`/`updatedAt`** — Add in same migration.

### Deferred (still open from prior rounds)

4. `cardService.ts:455` duplicateCard HTML write-back — verify `userId` in scope.
5. Booking `bookingController.ts:65-67` — UUID validation on `req.query.username`/`slug`.
6. `PricingPage.tsx` hardcoded personal email (`harshkeshari100@gmail.com`) — move to env var.
7. Composite indexes: Meeting, Task, CardContact, Booking, MeetingParticipant, MeetingShare.
8. `isDeleted`/`deletedAt` on Session, RefreshToken, OAuthAccount, TranscriptSegment — post-Phase 5 schema cleanup.
9. `notificationSubscriber.ts:49` Redis quit empty catch — add logger.warn.
10. ICS import batching, PDF/CSV export to queue, Task CRUD → taskService.ts.
