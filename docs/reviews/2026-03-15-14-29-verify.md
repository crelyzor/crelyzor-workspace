# Crelyzor Verification Report — 2026-03-15-14-29
Verifying against: `2026-03-14-11-39-review.md`

## Verdict: NOT READY — CRITICAL ISSUES REMAIN

21 new CRITICAL issues found. 27 of 28 previously fixed issues are confirmed fixed. The codebase has a confirmed compile error on `recordingController.ts` that makes all 4 recording endpoints non-functional.

---

## Fixed Issues ✅ (27 of 28 confirmed)

| Issue | File | Confirmed By |
|-------|------|--------------|
| Open redirect on `redirectUrl` | `googleController.ts` | Security agent: `isAllowedRedirectUrl()` allowlist present |
| `app.ts` global error handler used `console.error` + wrong shape | `app.ts` | Silent failures agent: uses `logger.error()` + `apiResponse()` |
| `deleteCard`/`duplicateCard` not transactional | `cardService.ts` | DB layer agent: both wrapped in `$transaction` |
| No character cap before OpenAI calls | `aiService.ts` | Performance agent: `MAX_PIPELINE_CHARS = 40000` confirmed |
| `aiController.ts` 4× `res.status().json()` bypassed convention | `aiController.ts` | API contract agent: all use `apiResponse()` |
| `speakerController.ts` 2× `res.status().json()` bypassed convention | `speakerController.ts` | Security agent: confirmed correct |
| `getMeetingById` ownership check post-fetch (IDOR) | `meetingController.ts` | Security agent: PASS |
| `deleteMeeting` no ownership scope (IDOR) | `meetingController.ts` | Security agent: PASS |
| Unused `getDeviceInfo` import, `catch (err: any)` | `authController.ts` | Tech debt agent: not re-flagged |
| Hardcoded `"gpt-4o-mini"`, no `OPENAI_MODEL` constant | `aiService.ts` | Tech debt agent: `const OPENAI_MODEL` present |
| Queue failure silently swallowed, status left as UPLOADED | `recordingService.ts` | Silent failures agent: catch block reverts status |
| Missing Zod schemas for 4 card operations | `cardSchema.ts` | API contract agent: not re-flagged |
| 4 card operations lacked Zod validation | `cardController.ts` | API contract agent: not re-flagged |
| `GCS_BUCKET_NAME` fell back to `"sma-dev"` | `storage.service.ts` | Security agent: throws on missing env var |
| Unbounded segments + tasks queries | `shareService.ts` | API contract agent: `take: 2000` and `take: 500` present |
| `on('failed')` handler missing `err.stack` | `config/queue.ts` | Silent failures: stack present |
| Localhost origins allowed in production | `corsOptions.ts` | Tech debt: guarded by `NODE_ENV !== "production"` ✅ |
| `logout` catch block swallowed all errors | `authService.ts` | Silent failures: only swallows JWT-specific errors |
| Unused `ActiveSession` import | `sessionService.ts` | Tech debt: not present in current file |
| Hardcoded `"nova-2"`, no `DEEPGRAM_MODEL` constant | `transcriptionService.ts` | Tech debt: `const DEEPGRAM_MODEL` present |
| Default tag color hardcoded in service | `tagService.ts` | Tech debt: `DEFAULT_TAG_COLOR` in schema, imported in service |
| `AskAITab` SSE stream not aborted on unmount | `SharedTabs.tsx` | Frontend agent: `abortRef` + cleanup confirmed |
| `handleSubmit` used manual state instead of `useMutation` | `SetupPage.tsx` | Frontend agent: `useMutation` confirmed |
| Used `VITE_CARDS_BASE_URL` instead of `VITE_CARDS_PUBLIC_URL` | `ShareSheet.tsx` | Frontend agent: correct env var used |
| `VITE_CARDS_BASE_URL` env key was wrong | `.env.local` | Frontend agent: confirmed updated |
| `setTimeout` reference not tracked in `Cards.tsx` | `Cards.tsx` | Frontend agent: `closeTimerRef` present |
| `useTranscript` + `useSummary` missing `staleTime` | `useSMAQueries.ts` | Performance agent: `staleTime: 10 * 60 * 1000` confirmed |
| `ErrorBoundary.tsx` plain `<button>`, `console.error` | `ErrorBoundary.tsx` | Frontend agent: shadcn `<Button>` used, no `console.error` |

---

## Still Present ❌ (1)

| Issue | File | Severity | Notes |
|-------|------|----------|-------|
| `deleteCard`/`duplicateCard` TOCTOU — `findUnique` before ownership check in app code | `cardService.ts:325,356` | HIGH | Transaction was added (previous fix), but the pre-fetch `findUnique` still has no `isDeleted: false` or `userId` in the DB `where` clause — a soft-deleted card can still be found and acted on. Now also part of the broader N-C11 pattern. |

---

## Regressions ⚠️ (0)

No regressions introduced. Previously fixed issues remain fixed.

---

## New Issues Found 🆕

### CRITICAL (21 issues)

| Issue | File | Severity | Recommendation |
|-------|------|----------|----------------|
| **Compile error**: imports `globalResponseHandler` which doesn't exist (should be `apiResponse`) — ALL 4 recording endpoints broken at runtime | `recordingController.ts:5` | CRITICAL | Replace import with `apiResponse`, update all 4 call sites |
| `findMany` on `MeetingRecording` missing `isDeleted: false` in `getRecordings` | `recordingService.ts:179` | CRITICAL | Add `isDeleted: false` to `where` clause |
| `findUnique` on `MeetingRecording` missing `isDeleted` guard in `transcribeRecording` | `recordingService.ts:40` | CRITICAL | Replace with `findFirst` + `isDeleted: false` |
| `findFirst` on `MeetingTranscript` missing `isDeleted: false` in `getTranscript` | `transcriptionService.ts:295` | CRITICAL | Add `isDeleted: false` to `where` clause |
| `findFirst` on `MeetingTranscript` missing `isDeleted: false` in `triggerAIProcessing` | `transcriptionService.ts:279` | CRITICAL | Add `isDeleted: false` to `where` clause |
| `findUnique` on `Meeting` missing `isDeleted` guard in `generateSummary` | `aiService.ts:35` | CRITICAL | Replace with `findFirst` + `isDeleted: false` |
| `findFirst` on `MeetingTranscript` missing `isDeleted: false` in `processTranscriptWithAI` | `aiService.ts:292` | CRITICAL | Add `isDeleted: false` to `where` clause |
| `findFirst` on `MeetingTranscript` missing `isDeleted: false` in `askAI` | `aiService.ts:379` | CRITICAL | Add `isDeleted: false` + `segments: { where: { isDeleted: false } }` |
| `findFirst` on `MeetingTranscript` missing `isDeleted: false` in `generateContent` | `aiService.ts:494` | CRITICAL | Add `isDeleted: false` to `where` clause |
| `findUnique` on `MeetingSpeaker` missing `isDeleted` guard in `renameSpeaker` | `speakerService.ts:42` | CRITICAL | Replace with `findFirst` + `meetingId, isDeleted: false` in DB `where` |
| `getCardById`, `updateCard`, `deleteCard`, `duplicateCard`, `getCardAnalytics` — ownership checked in app code post-fetch (TOCTOU) | `cardService.ts:192,210,325,356,661` | CRITICAL | Use `findFirst({ where: { id: cardId, userId } })` in all 5 methods |
| `getUserCards` `findMany` missing `isDeleted: false` | `cardService.ts:179` | CRITICAL | Add `isDeleted: false` to `where` clause |
| `updateContactTags`, `deleteContact` — ownership verified in app code post-fetch (TOCTOU) | `cardService.ts:596,613` | CRITICAL | Use `findFirst({ where: { id: contactId, userId } })` |
| `findMany` on `MeetingSpeaker` missing `isDeleted: false` in public meeting endpoint | `shareService.ts:154` | CRITICAL | Add `isDeleted: false` to `where` clause |
| `findFirst` on `MeetingTranscript` missing `isDeleted: false` in `buildTranscriptText` | `exportService.ts:14` | CRITICAL | Add `isDeleted: false`, also add `isDeleted: false` to segments include |
| `findFirst` on `MeetingAISummary` missing `isDeleted: false` in `buildSummaryText` | `exportService.ts:32` | CRITICAL | Add `isDeleted: false` to `where` clause |
| `findMany` on `MeetingNote` missing `isDeleted: false` in `getNotes` | `aiController.ts:131` | CRITICAL | Add `isDeleted: false` to `where` clause |
| **Hard delete** on `MeetingNote` via `prisma.meetingNote.delete` — violates soft-delete architecture | `aiController.ts:270` | CRITICAL | Replace with `update({ isDeleted: true, deletedAt: new Date() })` |
| `redisClient.ts` throws synchronously at module load on missing env vars — crashes server before startup code runs | `config/redisClient.ts` | CRITICAL | Move guard to lazy init function; throw `AppError` only on first use |
| `audioCompression.ts` silently falls back to original file on any error including potential shell injection via unescaped filename | `utils/audio/audioCompression.ts` | CRITICAL | Use `execFile` instead of `exec`; surface compression failures explicitly |
| `autoRefreshToken` catches ALL errors and calls `next()` — non-JWT errors silently corrupt auth header state | `middleware/authMiddleware.ts:85` | CRITICAL | Only swallow `JsonWebTokenError` / `TokenExpiredError`; propagate others via `next(error)` |

### HIGH (40 issues)

| Issue | File | Severity | Recommendation |
|-------|------|----------|----------------|
| `cancelMeeting` allows any participant to cancel (not just creator/organizer) | `meetingService.ts` | HIGH | Restrict to `isCreator || isOrganizer` |
| `storage.controller.ts` — 5 endpoints read `req.body` without Zod validation | `storage.controller.ts` | HIGH | Create `storageSchema.ts`, replace manual guards |
| Public meeting always returns speaker names even when `showTranscript` is false | `shareService.ts:154` | HIGH | Gate speaker fetch on `showTranscript` flag |
| `userRateLimit` uses in-process `Map` — not safe for multi-instance deployment | `middleware/authMiddleware.ts` | HIGH | Migrate to Redis sliding window (same as `checkAskAIRateLimit`) |
| Bare `prisma.meeting.update` outside transaction in error handler | `recordingService.ts:140` | HIGH | Wrap status revert in its own `try/catch` with `logger.error` |
| Multiple sequential writes with no transaction in `regenerateSummary` | `aiService.ts:54–81` | HIGH | Wrap summary + keyPoints upserts in `$transaction` |
| `createCard` has 3 sequential writes with no transaction | `cardService.ts:118–172` | HIGH | Wrap all 3 writes in `$transaction` |
| `getCardAnalytics` fetches ALL `CardView` rows into memory for JS aggregation | `cardService.ts:669` | HIGH | Use `prisma.groupBy` / `prisma.$queryRaw` for DB-side aggregation |
| No `take` limit on `MeetingNote` `findMany` | `aiController.ts:131` | HIGH | Add `take: 500` |
| Queue init failure silently swallowed; runtime queue calls throw 500s | `index.ts:37` | HIGH | Fail hard on queue init or return explicit error from upload endpoint when queue unavailable |
| `extractKeyPoints`/`extractTasks` return empty `[]` on JSON parse failure; raw GPT content not logged | `aiService.ts:122,200` | HIGH | Log raw `content` at error level before returning `[]` |
| `generateMeetingTitle` in `Promise.all` — fire-and-forget with no error visibility | `aiService.ts:300` | HIGH | Log on failure; or remove from `Promise.all` and run as true background operation |
| Queue failure after DB commit returns 201 success with no transcription queued | `recordingService.ts:119` | HIGH | Re-throw after status revert; return 500 or explicit partial-success response |
| Google user info error throws plain `Error` (not `AppError`) — untracked 500 | `googleService.ts:38` | HIGH | Replace with `throw new AppError("Google account has no email", 422)` |
| `new URL()` throws unhandled `TypeError` on malformed URL in `addLink` | `attachmentService.ts:138` | HIGH | Ensure `addLinkSchema` uses `z.string().url()` |
| Global error handler exposes `err.message` directly in production | `app.ts:44` | HIGH | Gate message on `NODE_ENV !== "production"` |
| Missing `refreshToken` silently stored in `AuthCallback.tsx` — causes unexpected logout | `AuthCallback.tsx:19` | HIGH | Show error or warn when `refreshToken` absent |
| Token refresh bare `catch {}` returns `false` with no logging | `apiClient.ts:74` | HIGH | Log error before returning `false` |
| Delete Account button has no `onClick` handler — does nothing | `Settings.tsx:405` | HIGH | Add handler or remove the button |
| No `componentDidCatch` on `ErrorBoundary` — render errors never logged | `ErrorBoundary.tsx` | HIGH | Add `componentDidCatch(error, info)` with `logger` or `console.error` |
| `getMeetingsWithoutPagination` — no enforced maximum date span | `meetingSchema.ts` | HIGH | Add max span refinement to Zod schema |
| Response shape: backend returns `{status: "success"}` vs documented `{success: true}` | Various | HIGH | Align documentation or normalize shape |
| `meetingController.ts` `getMeetingById` bypasses service layer | `meetingController.ts` | HIGH | Move query to `meetingService.getMeetingById()` |
| `createNote` has inline Zod schema inside controller function body | `aiController.ts:152` | HIGH | Extract to `noteSchema.ts` validator file |
| `GET /cards` no pagination | `cardService.ts` | HIGH | Add defensive `take: 50` |
| `GET /sma/meetings/:id/notes` no pagination | `aiController.ts` | HIGH | Add `take: 500` or pagination |
| Wrong fallback port `localhost:4000` vs actual backend port `3000` | `crelyzor-public/CardView.tsx:139` | HIGH | Fix to `localhost:3000` |
| Hardcoded personal dev URL `https://cal.harsh.dev/book/harsh` | `Toolbar.tsx:29` | HIGH | Source from user profile or env var |
| `transcriptionService.ts` over-fetches full `Meeting` when only `meetingId` needed | `transcriptionService.ts` | HIGH | Replace `include: { meeting: true }` with `select: { meetingId: true }` |
| `transcriptionService.ts` over-fetches full `MeetingRecording` | `transcriptionService.ts` | HIGH | Replace `include: { recording: true }` with `select: { recording: { select: { id: true } } }` |
| `regenerateSummary` calls 2 OpenAI completions synchronously in HTTP request | `aiController.ts` | HIGH | Queue to Bull; return 202 |
| `regenerateTitle` calls OpenAI synchronously in HTTP request | `aiController.ts` | HIGH | Queue to Bull; return 202 |
| PDF generation blocks event loop synchronously | `exportController.ts` | HIGH | Offload to worker thread or queue |
| `getTranscript` no `take` limit on segments | `transcriptionService.ts` | HIGH | Add `take: 2000` |
| `CardAnalytics.tsx` no error state | `CardAnalytics.tsx` | HIGH | Add `isError` guard with retry |
| `Home.tsx` no error state for meetings feed | `Home.tsx` | HIGH | Propagate `isError` to `RecentMeetings` |
| `Settings.tsx` `ProfileSection` — `setState` in render body (React anti-pattern) | `Settings.tsx:139` | HIGH | Convert to `useEffect([profile])` |
| `SetupPage.tsx` username check uses `useEffect` + direct API call | `SetupPage.tsx` | HIGH | Convert to `useQuery` with `enabled` |
| `smaService.regenerateSummary` shape mismatch — backend/frontend contract mismatch | `aiController.ts:69` / `smaService.ts:191` | HIGH | Align return shape |
| No rate limiting on `GET /public/card/:username` | `publicCardRoutes.ts` | HIGH | Apply `clickLimiter` or dedicated `readLimiter` |

### MEDIUM (28 issues)

Schema (Agent 3): Missing `isDeleted`/`deletedAt` on 16 models (`Session`, `LoginHistory`, `RefreshToken`, `OAuthAccount`, `MeetingParticipant`, `MeetingStateHistory`, `MeetingRecording`, `MeetingTranscript`, `TranscriptSegment`, `MeetingSpeaker`, `MeetingNote`, `MeetingAISummary`, `MeetingAIContent`, `Card`, `CardContact`, `CardView`); dangling FKs on `MeetingNote.author`, `MeetingRecording.uploadedBy`, `User.deletedById`, `Meeting.deletedBy`; `RefreshToken.token` missing `@unique`/index.

Tech Debt: 6× `as any` in `meetingController.ts`; 6× `as any` + `Promise<any[]>` in `meetingService.ts`; `jwt.decode as any` in `tokenService.ts`; unused imports in `deepgram.ts`, `tokenService.ts`; unreferenced `session` variable in `sessionService.ts`; unused `folder` param in `storage.service.ts`; `error?: any` in `globalErrorHandler.ts`.

API Contract: `MeetingStatus` type drift; `Meeting.guests[]` never returned; `Meeting.recording?` never returned; `GET /tags` no cap; `authController.ts getSessions` no pagination.

Frontend: `SharedTabs.tsx GenerateTab` `setTimeout` not cleaned up on unmount; `VoiceNotes.tsx` no error state; `CardContacts.tsx` no error state; `Meetings.tsx` no error state; dead `transcriptionStatus` prop in `SharedTabs.tsx`.

Performance: Missing `staleTime` on `useCardAnalytics`, `useCardContacts`, `useTasks`, `useNotes`, `useSpeakers`, `useMeetingAttachments`, `useMeetingTags`; `SegmentRow` not in `React.memo`; PDFKit top-level import; `getMeetingsWithoutPagination` 200-row silent truncation.

---

## Recommended Next Action

Run `/crelyzor-review` again to address the critical findings. Priority order:

1. **Fix `recordingController.ts` compile error immediately** — all recording features are currently broken.
2. **Add `isDeleted: false` guards to all Prisma queries** — affects `recordingService`, `transcriptionService`, `aiService`, `cardService`, `shareService`, `exportService`, `aiController` (17 individual query sites).
3. **Fix `aiController.ts:270` hard delete** — the only hard delete outside the `HARD_DELETE_ENABLED` guard in the codebase.
4. **Fix `redisClient.ts` module-level throw** and **`autoRefreshToken` catch-all** — both crash or corrupt server state silently.
5. After CRITICAL fixes, address HIGH items (security: cancelMeeting, storage Zod, speaker data leak, rate limit; performance: sync OpenAI in request path).
