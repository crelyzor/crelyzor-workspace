# Crelyzor Production Review — 2026-03-16-14-03

## Summary
- Total issues found: 26 (1 critical, 0 high, 22 medium, 3 low)
- Total issues fixed: 26
- Issues skipped: 0
- Session: Continuation of 2026-03-16-02-10 — addressed isDeleted soft-delete gaps, TOCTOU race in deleteMeeting, and frontend error states

---

## Issues Found & Fixed

### CRITICAL

| # | File | Issue | Status |
|---|------|-------|--------|
| 1 | `src/controllers/recordingController.ts` | All 4 recording endpoints crashed at runtime — imported `globalResponseHandler` which doesn't exist as an export (only `apiResponse` is exported) | ✅ Fixed — rewrote controller to use `apiResponse` with correct signature, removed useless try/catch-rethrow blocks |

### MEDIUM — isDeleted Soft-Delete Gaps

| # | File | Issue | Status |
|---|------|-------|--------|
| 2 | `src/services/cardService.ts` | `createCard` slug check used `findUnique` — deleted cards with same slug blocked new card creation | ✅ Fixed — changed to `findFirst({ where: { userId, slug, isDeleted: false } })` |
| 3 | `src/services/cardService.ts` | `createCard` cardCount included deleted cards — wrong default detection | ✅ Fixed — added `isDeleted: false` to count |
| 4 | `src/services/cardService.ts` | `getUserCards` returned deleted cards | ✅ Fixed — added `isDeleted: false` |
| 5 | `src/services/cardService.ts` | `getCardById` used `findUnique` without ownership in query (app-layer check) | ✅ Fixed — changed to `findFirst({ where: { id, userId, isDeleted: false } })` |
| 6 | `src/services/cardService.ts` | `updateCard` used `findUnique` without ownership or soft-delete in query | ✅ Fixed — changed to `findFirst` with `userId, isDeleted: false` |
| 7 | `src/services/cardService.ts` | `updateCard` slug conflict check included deleted cards | ✅ Fixed — added `isDeleted: false` |
| 8 | `src/services/cardService.ts` | `deleteCard` used `findUnique` without ownership, then hard-deleted the record | ✅ Fixed — changed to `findFirst` with ownership; changed `delete` to soft-delete `update({ isDeleted: true, deletedAt })` |
| 9 | `src/services/cardService.ts` | `deleteCard` next-card promotion included deleted cards | ✅ Fixed — added `isDeleted: false` to `findFirst` |
| 10 | `src/services/cardService.ts` | `duplicateCard` ownership check used `findUnique` without soft-delete | ✅ Fixed — changed to `findFirst` with `userId, isDeleted: false` |
| 11 | `src/services/cardService.ts` | `duplicateCard` slug conflict check included deleted cards | ✅ Fixed — added `isDeleted: false` |
| 12 | `src/services/cardService.ts` | `getCardAnalytics` used `findUnique` without ownership or soft-delete | ✅ Fixed — changed to `findFirst` with `userId, isDeleted: false` |
| 13 | `src/services/cardService.ts` | `getPublicCard` (slug path) used `findUnique` — could serve deleted card | ✅ Fixed — changed to `findFirst({ where: { userId, slug, isDeleted: false } })` |
| 14 | `src/services/cardService.ts` | `getPublicCard` default/fallback paths missing `isDeleted: false` | ✅ Fixed — added `isDeleted: false` to both `findFirst` calls |
| 15 | `src/services/cardService.ts` | `submitContact` used `findUnique` without `isDeleted: false` | ✅ Fixed — changed to `findFirst({ where: { id, isDeleted: false } })` |
| 16 | `src/services/ai/aiService.ts` | `generateSummary` used `findUnique` — could read deleted meeting's title | ✅ Fixed — changed to `findFirst({ where: { id, isDeleted: false } })` |

### MEDIUM — TOCTOU / Transaction Gaps

| # | File | Issue | Status |
|---|------|-------|--------|
| 17 | `src/controllers/meetingController.ts` | `deleteMeeting` had a TOCTOU race — `findFirst` then separate `update` could conflict | ✅ Fixed — replaced with atomic `updateMany` that includes ownership predicate in `where`, returns count 0 → 404 |

### MEDIUM — Frontend Error States

| # | File | Issue | Status |
|---|------|-------|--------|
| 18 | `src/pages/meetings/Meetings.tsx` | Missing `isError` state — API failure showed empty list with no feedback | ✅ Fixed — added error state with "Failed to load meetings" message |
| 19 | `src/pages/voice-notes/VoiceNotes.tsx` | Missing `isError` state | ✅ Fixed — added error state |
| 20 | `src/pages/card-analytics/CardAnalytics.tsx` | Missing `isError` state | ✅ Fixed — added early-return error state before loading skeleton |
| 21 | `src/pages/cards/Cards.tsx` | Missing `isError` state | ✅ Fixed — added error state as outer condition in ternary chain |
| 22 | `src/pages/home/Home.tsx` + `RecentMeetings.tsx` | Missing `isError` propagation to meeting widget | ✅ Fixed — added `isError` prop to `RecentMeetings`, passed from `Home` |
| 23 | `src/pages/card-contacts/CardContacts.tsx` | Missing `isError` state | ✅ Fixed — added error state |

### LOW — Error Boundaries

| # | File | Issue | Status |
|---|------|-------|--------|
| 24 | `src/pages/meeting-detail/RecordedDetail.tsx` | No per-tab `ErrorBoundary` — a render crash in any tab crashed the whole meeting page | ✅ Fixed — wrapped all 7 `TabsContent` bodies in `<ErrorBoundary fallback={...}>` |
| 25 | `src/pages/meeting-detail/RecordedDetail.tsx` | Missing `ErrorBoundary` import | ✅ Fixed — added import from `@/components/ErrorBoundary` |

---

## Files Changed

**Backend (`crelyzor-backend`)**
- `src/controllers/recordingController.ts` — fixed broken `globalResponseHandler` import → `apiResponse`
- `src/services/cardService.ts` — 14 soft-delete gaps fixed; `deleteCard` now soft-deletes
- `src/services/ai/aiService.ts` — `generateSummary` now uses `findFirst` with `isDeleted: false`
- `src/controllers/meetingController.ts` — `deleteMeeting` TOCTOU fixed with atomic `updateMany`

**Frontend (`crelyzor-frontend`)**
- `src/pages/meetings/Meetings.tsx` — added `isError` state
- `src/pages/voice-notes/VoiceNotes.tsx` — added `isError` state
- `src/pages/card-analytics/CardAnalytics.tsx` — added `isError` state
- `src/pages/cards/Cards.tsx` — added `isError` state
- `src/pages/home/Home.tsx` — added `isError` from query, passes to `RecentMeetings`
- `src/pages/home/RecentMeetings.tsx` — added `isError` prop + error state rendering
- `src/pages/card-contacts/CardContacts.tsx` — added `isError` state
- `src/pages/meeting-detail/RecordedDetail.tsx` — added `ErrorBoundary` import + wrapped all 7 tabs

---

## Next Steps

1. **Run `pnpm db:push`** in `crelyzor-backend` — not needed (no schema changes in this session)
2. **Verify soft-delete on `deleteCard`** — the endpoint now soft-deletes instead of hard-deletes. Cards with `isDeleted: true` will no longer appear in `getUserCards`, `getPublicCard`, or slug conflict checks. Confirm this is the desired behavior (vs. the previous hard delete).
3. **Run `/crelyzor-verify`** to confirm all fixes from both sessions are in place
