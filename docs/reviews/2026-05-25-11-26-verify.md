# Crelyzor Verification Report — 2026-05-25 11:26
Verifying against: 2026-05-25-11-04-review.md

## Verdict: NOT READY — CRITICAL ISSUES REMAIN

---

## Fixed Issues ✅ (18/18 confirmed)

| Issue | File | Confirmed By |
|-------|------|--------------|
| generateSummary missing createdById:userId | aiService.ts:82 | Security agent |
| generateSummaryAndKeyPoints missing createdById:userId | aiService.ts:233 | Security agent |
| bookingManagementService.ts 3× unscoped update → updateMany({id,userId}) | bookingManagementService.ts:166,449,566 | DB layer agent |
| deleteTask unscoped update → updateMany({id,userId,isDeleted:false}) | taskController.ts:717 | DB layer agent |
| GCAL_PUSH_SYNC catch return {success:false} → throw err | jobProcessor.ts:755-762 | Silent failures agent |
| handleBulkDelete forEach mutate → Promise.allSettled + mutateAsync | CardContacts.tsx:191-204 | Frontend agent |
| handleBulkTag hardcoded queryKeys → queryKeys.cards.contacts() / queryKeys.tags.all | CardContacts.tsx:220-221 | Frontend agent |
| apiResponse missing return × 4 | aiController.ts:96,141,302,321 | API contract agent |
| meetingSchema limit no .max() cap | meetingSchema.ts:77 | API contract agent |
| handleBulkComplete/Delete/Priority Promise.all → Promise.allSettled | Tasks.tsx:533-587 | Frontend agent |
| updateMeeting GCal catch empty → logger.warn | meetingService.ts:612 | Silent failures agent |
| Meeting type phantom fields guestEmail/guestName/guestMessage removed | types/meeting.ts:87-89 | API contract agent |
| keyPoints decrypt failure silent → .catch + logger.warn | aiController.ts:40-50 | Silent failures agent |
| TaskPriority not exported → exported from @/types/meeting | types/meeting.ts | Frontend agent |
| BulkActionBar local Priority re-declaration → import TaskPriority | BulkActionBar.tsx:5 | Frontend agent |
| CreateTaskModal local Priority re-declaration → import TaskPriority | CreateTaskModal.tsx:14 | Frontend agent |
| SetupPage VITE_CARDS_PUBLIC_URL fallback prod URL → localhost:5174 | SetupPage.tsx:11 | Tech debt agent |
| PricingPage VITE_CARDS_PUBLIC_URL fallback prod URL → localhost:5174 | PricingPage.tsx:19 | Tech debt agent |
| corsOptions.ts stale localhost:3000 removed | corsOptions.ts:10 | Security agent |

---

## Still Present ❌

None of the previously fixed issues regressed.

---

## Regressions ⚠️

None detected.

---

## New Issues Found 🆕

### CRITICAL

| # | Issue | File | Severity | Recommendation |
|---|-------|------|----------|----------------|
| N1 | `verifyContactOwnership` calls `prisma.cardContact.findFirst` without `isDeleted: false` — soft-deleted contacts can receive tags | `tagService.ts` ~line 447 | CRITICAL | Add `isDeleted: false` to the findFirst where clause |

### HIGH

| # | Issue | File | Severity | Recommendation |
|---|-------|------|----------|----------------|
| N2 | `updateTask` uses `task.update({where:{id,userId}})` without `isDeleted:false` — race window allowing update of soft-deleted task | `taskController.ts:524` | HIGH | Change to `updateMany({where:{id,userId,isDeleted:false}})` |
| N3 | `getPublicBooking` fetches booking then checks `username`/`slug` post-fetch rather than in DB query — TOCTOU | `bookingService.ts:676` | HIGH | Move username/slug filter into the Prisma where clause |
| N4 | `parseMeetingId` helper throws 404 for invalid UUID format — should be 400 (Bad Request) | `meetingController.ts` | HIGH | Change to `throw new AppError("Invalid meeting ID", 400)` |
| N5 | `getTasks` response missing `taskTags` include — inconsistent with `getAllTasks` (frontend may silently have no tags on tasks page) | `taskController.ts` | HIGH | Add `taskTags: { include: { tag: true } }` to the include |
| N6 | `CardUser.id` is required in `crelyzor-public/src/types/card.ts` but `getPublicCard` never returns it | `crelyzor-public/src/types/card.ts` | HIGH | Make `CardUser.id` optional or add it to backend response |
| N7 | `TagsPage.tsx` has no `isError` check — silently renders empty on API failure with no feedback | `TagsPage.tsx` | HIGH | Add error state branch |
| N8 | `Bookings/index.tsx` error state exists but has no retry button | `Bookings/index.tsx` | HIGH | Add retry button calling `refetch()` |
| N9 | `useCreateStandaloneTask` mutation missing `toast.success` on success | `useSMAQueries.ts` | HIGH | Add `onSuccess: () => toast.success("Task created")` |
| N10 | `useUpdateProfile` mutation missing `onError` and `onSuccess` feedback | `useUserQueries.ts` | HIGH | Add both callbacks with toast |
| N11 | Tasks.tsx bulk ops invalidate `queryKeys.sma.allTasks()` only — per-meeting task caches not busted | `Tasks.tsx` | HIGH | Invalidate `queryKeys.sma.all` (prefix match) or add per-meeting invalidation |
| N12 | `generateMeetingTitle` calls `prisma.meeting.update({where:{id}})` without userId scope — latent IDOR (callers verify ownership, but defence-in-depth missing) | `aiService.ts` ~line 488 | HIGH | Add `createdById: userId` to where clause |
| N13 | Schema: 7 missing indexes — Meeting.endTime, MeetingAttachment.meetingId, EventType.userId, AvailabilitySchedule.userId, Availability.scheduleId, Booking.endTime, Notification.[entityType,entityId] | `prisma/schema.prisma` | HIGH | Add indexes in next migration |
| N14 | `bookingManagementService.ts` post-confirm/decline/cancel `findUnique` calls missing `isDeleted:false` and `userId` scope at lines 410, 521, 658 | `bookingManagementService.ts` | HIGH | Scope to `where:{id, userId, isDeleted:false}` |

### MEDIUM

| # | Issue | File | Severity | Recommendation |
|---|-------|------|----------|----------------|
| N15 | `taskController.ts` secondary updates at lines 588, 595, 617, 619 without `isDeleted:false` | `taskController.ts` | MEDIUM | Add `isDeleted:false` to all task update where clauses |
| N16 | `reorderTasks` uses `tx.task.update` without `isDeleted:false` | `taskController.ts` | MEDIUM | Add `isDeleted:false` |
| N17 | `processTranscriptWithAI` runs Gemini summary and key-point calls sequentially — should be `Promise.all` | `aiService.ts` | MEDIUM | Parallelize with `Promise.all([generateSummary, extractKeyPoints])` |
| N18 | `askAI` decrypts all TranscriptSegments before context filter — should use pre-built `transcript.fullText` | `aiService.ts` | MEDIUM | Use `fullText` for context rather than decrypting individual segments |
| N19 | `getCardMeetings` has no pagination — unbounded result set | `cardService.ts` | MEDIUM | Add `skip`/`take` with cursor-based pagination |
| N20 | `tagService.ts verifyOwnership` runs 3 sequential ownership queries — can be merged or parallelized | `tagService.ts` | MEDIUM | Use `Promise.all` for parallel verification |
| N21 | `importContactsFromCsv` encrypts contacts sequentially — should batch | `cardService.ts` | MEDIUM | Encrypt concurrently with `Promise.all` |
| N22 | `generateSummary` fetches full `Meeting` row — should `select: { title:true, description:true }` | `aiService.ts` | MEDIUM | Add minimal select projection |

### LOW

| # | Issue | File | Severity | Recommendation |
|---|-------|------|----------|----------------|
| N23 | `getContacts` decrypts email on every fetch with no LRU cache | `cardService.ts` | LOW | Add in-process LRU cache for decrypted emails (Phase 5 task) |
| N24 | Schema: `MeetingParticipant` missing `@@index([userId, meetingId])` — OR query does full scan | `prisma/schema.prisma` | LOW | Add composite index in next migration |

---

## Summary

| Category | Count |
|----------|-------|
| Previously fixed (confirmed) | 18 / 18 |
| Regressions | 0 |
| New CRITICAL | 1 |
| New HIGH | 13 |
| New MEDIUM | 8 |
| New LOW | 2 |

---

## Recommended Next Action

Run `/crelyzor-review` to fix the new issues, prioritized as:

1. **CRITICAL first:** `tagService.ts verifyContactOwnership` — add `isDeleted:false` to CardContact findFirst (1-line fix)
2. **HIGH security:** `taskController.ts:524` updateTask race window, `aiService.ts generateMeetingTitle` missing userId scope, `bookingManagementService.ts` post-op findUnique scoping
3. **HIGH frontend quality:** TagsPage error state, Bookings retry, useCreateStandaloneTask toast, useUpdateProfile callbacks, Tasks.tsx cache invalidation
4. **HIGH API contract:** parseMeetingId 400 vs 404, getTasks taskTags include, CardUser.id optional
5. **Schema indexes** (require migration — flag as manual action if not auto-applicable)
