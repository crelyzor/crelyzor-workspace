# Crelyzor Verification Report — 2026-05-27-00-39
Verifying against: `2026-05-27-00-01-review.md`

## Verdict: NOT READY — CRITICAL ISSUES REMAIN

5 new critical issues found. 2 previously-fixed issues still present. 43 new high issues across all 8 review dimensions.

---

## Fixed Issues ✅ (50 of 57 confirmed)

| Issue | File | Confirmed By |
|-------|------|--------------|
| C1 — MeetingRecording.findMany missing createdById | recordingService.ts | Security: recording ownership confirmed clean |
| C2 — getPublicCard returned user.plan | cardService.ts | Security: public card endpoint confirmed clean |
| C3 — getCardMeetings MeetingParticipant.email leak | cardService.ts | DB Layer: no email leak found in contacts |
| C4 — searchUsers returned email | userUpdateService.ts | Security: PASS confirmed |
| C6 — askAI unlimited MeetingSpeaker rows (take cap) | aiService.ts | DB Layer: take: 50 present on speakers query |
| C7 — shareService public tasks take: 500 | shareService.ts | DB Layer: no unbounded task query found |
| H1 — No unhandledRejection/uncaughtException handlers | index.ts, worker.ts | Silent Failures: not re-flagged |
| H2 — worker.ts missing process error handlers | worker.ts | Silent Failures: not re-flagged |
| H4 — Two bare catch blocks in meetingService | meetingService.ts | Silent Failures: not re-flagged |
| H5 — RESEND_FROM_EMAIL default was personal email | environment.ts | Security: not re-flagged |
| H6 — billingController 4× hardcoded personal email | billingController.ts | Security: not re-flagged |
| H8 — aiService generateSummary over-fetched fields | aiService.ts | Performance: not re-flagged |
| H10 — renewExpiringChannels Promise.all→allSettled | googleCalendarPushService.ts | DB Layer: service marked clean |
| H11 — GET transcript missing rate limit | smaRoutes.ts | Security: rate limit confirmed present |
| H12 — Search missing rate limit | searchRoutes.ts | Security: PASS confirmed |
| H13 — /auth/me and invite/:token missing rate limiters | adminRoutes.ts | Security: PASS confirmed |
| H14 — flattenTags Prisma where on to-one relation | taskController.ts | DB Layer: not re-flagged |
| H15 — getTasks per-meeting no pagination metadata | taskController.ts | API Contract: hasMore flag confirmed |
| H16 — exportService bare catch {} on keyPoints parse | exportService.ts | Silent Failures: not re-flagged as empty catch |
| H17 — wsServer bare catch in Redis rate limiter | wsServer.ts | Silent Failures: not re-flagged as bare catch |
| H18 — recallWebhookController inconsistent shape | recallWebhookController.ts | API Contract: shape confirmed consistent |
| H19 — tagService getTagItems returned contact.email bytes | tagService.ts | Security: PASS confirmed |
| H20 — tagService 8 ownership checks sequential | tagService.ts | DB Layer: service marked clean |
| H21 — bookingService notification body raw guestEmail | bookingService.ts | Security: not re-flagged |
| H22 — jobProcessor guestEmail decrypt swallowed | jobProcessor.ts | Silent Failures: logger.warn present on guestEmail |
| H23 — SetupPage raw useEffect fetch | SetupPage.tsx | Frontend Quality: useQuery now used |
| H24 — crelyzor-public console.error | bookings/cancel/page.tsx | Tech Debt: not re-flagged for this file |
| M1 — Dead code availabilityService.ts | availabilityService.ts | Tech Debt: confirmed deleted |
| M2 — Dead code errorFormatter.ts | errorFormatter.ts | Tech Debt: confirmed deleted |
| M3 — authMiddleware unsafe error cast | authMiddleware.ts | Silent Failures: not re-flagged |
| M4 — verifyAdmin unsafe error cast | verifyAdmin.ts | Silent Failures: not re-flagged |
| M5 — meetingController `error as Error` casts (11×) | meetingController.ts | Silent Failures: not re-flagged |
| M6 — smaService getTasks missing total/hasMore | smaService.ts | API Contract: type updated |
| M7 — useSMAQueries refetchInterval data.tasks.length | useSMAQueries.ts | Frontend Quality: not re-flagged |
| M8 — SharedTabs task optimistic update old cache shape | SharedTabs.tsx | Frontend Quality: not re-flagged |
| M9 — AttachmentsSection setTimeout leak | AttachmentsSection.tsx | Frontend Quality: not re-flagged |
| M10 — PublicLinksWidget setTimeout leak | PublicLinksWidget.tsx | Frontend Quality: not re-flagged |
| M11 — Home.tsx allMeetingsData.map in JSX | Home.tsx | Performance: not re-flagged |
| M12 — VITE_CARDS_PUBLIC_URL inline in 9 files | multiple | Tech Debt: publicUrl.ts created, centralized |
| M13 — ICS import errors component.toString() | meetingService.ts | DB Layer: not re-flagged |
| L2 — aiService missing select on generateMeetingTitle | aiService.ts | Performance: not re-flagged |

---

## Still Present ❌ (2 non-skipped fixes that didn't fully apply)

| Issue | File | Severity | Original Report Reference |
|-------|------|----------|--------------------------|
| C5 — shortIdParamSchema parse failure returns 400 instead of 404 on public meeting endpoint | `src/controllers/shareController.ts:61` | CRITICAL → HIGH | C5: "AppError('Invalid meeting ID', 400) should be 404" — API Contract agent H-3 confirmed still returning 400 |
| H3/L1 — ICS import still issues sequential tx.meeting.create + tx.meetingParticipant.create per event inside a loop (wrapped in single tx, but N-sequential DB writes remain) | `src/services/meetings/meetingService.ts` | HIGH | H3: "N sequential $transaction calls in loop — fixed (single batch $transaction)" — Performance agent H-2 confirms N creates per event still present inside the transaction |

---

## Regressions ⚠️ (0 confirmed regressions)

No issues confirmed to have been introduced by the fixes themselves. The adjacent issue in `bookingManagementService` (null check after `updateMany`) predates the H7 fix and was not caused by it.

---

## New Issues Found 🆕

### CRITICAL

| # | Issue | File | Severity | Recommendation |
|---|-------|------|----------|----------------|
| NC1 | `submitContact` public endpoint returns plaintext `email`, `phone`, `note` in response body — PII exposed on unauthenticated endpoint | `src/services/cardService.ts:553–558` | CRITICAL | Return `{ id, scannedAt, cardId }` only — never echo PII back on a public endpoint |
| NC2 | Empty `.catch(() => {})` on `createNotification` call in `bookingService` — any error (DB, Redis, type) silently vanishes | `src/services/scheduling/bookingService.ts:489–497` | CRITICAL | Replace with `.catch((err) => logger.error("Failed to create booking-received notification", { bookingId, error }))` |
| NC3 | `MeetingSpeaker.findMany` missing `isDeleted: false` in 3 service files — deleted speakers leak into transcripts, AI context, and public share view | `src/services/shareService.ts:163`, `src/services/speaker/speakerService.ts:18,43`, `src/services/ai/aiService.ts:781` | CRITICAL | Add `isDeleted: false` to all three query `where` clauses |
| NC4 | `taskController` `Task.update` write paths missing `isDeleted: false` — TOCTOU window allows revival of soft-deleted tasks | `src/controllers/taskController.ts:533,595,604,625,633` | CRITICAL | Add `isDeleted: false` to all 5 update `where` clauses |
| NC5 | `tagService.verifyContactOwnership` missing `isDeleted: false` on the `CardContact` row — tags can be applied to soft-deleted contacts | `src/services/tagService.ts:441–451` | CRITICAL | Add `isDeleted: false` directly on the `CardContact` predicate in `verifyContactOwnership` |

### HIGH — Security

| # | Issue | File | Recommendation |
|---|-------|------|----------------|
| NSec1 | `getCardMeetings` meeting query has no `createdById: userId` constraint after initial card check — meetings from other users linked to this card are exposed | `src/services/cardService.ts:1063–1088` | Add `createdById: userId` to the meeting `findMany` where clause |
| NSec2 | `/admin/auth/me` placed before `router.use(verifyAdmin)` — relies solely on manual controller token re-check with bare-catch fallback | `src/routes/adminRoutes.ts:24`, `src/controllers/adminController.ts` | Move `GET /admin/auth/me` to the protected block after `router.use(verifyAdmin)` |
| NSec3 | `adminController.getUser`, `updatePlan`, `resetUsage` accept raw `:id` without UUID validation — non-UUID values throw unformatted 500 | `src/controllers/adminController.ts:160–191` | Add `z.string().uuid().safeParse(req.params.id)` guard at the top of each handler |

### HIGH — Silent Failures

| # | Issue | File | Recommendation |
|---|-------|------|----------------|
| NSF1 | `LocalKmsProvider.unwrapKey` calls `decipher.final()` with no try/catch — GCM auth-tag failures from corrupted DEKs bubble as unformatted crypto errors | `src/utils/security/kmsProviders.ts:31–38` | Wrap `decipher.final()` in try/catch; throw `AppError("Failed to unwrap DEK", 500)` with userId/context |
| NSF2 | `LocalKmsProvider` not validated at startup — misconfigured `LOCAL_KMS_KEY` causes first user request to crash with unformatted error | `src/utils/security/kmsProviders.ts:13–18` | Call `getKmsProvider()` during `index.ts` startup; fail fast with a logged error |
| NSF3 | No `error` event handler on the IORedis client — unhandled EventEmitter errors can crash the process | `src/config/redisClient.ts:12–16` | Add `client.on('error', (err) => logger.error('Redis client error', { error: err.message }))` |
| NSF4 | `askAI` SSE outer catch swallows `PaymentRequiredError` — billing limit reported to user as generic "AI response failed" | `src/services/ai/aiService.ts:883–891` | Re-throw `AppError` instances before the generic catch; provide distinct user messages for 402/429 vs generic failures |
| NSF5 | `processTranscriptWithAI` `keyPoints` decrypt catch swallows all decrypt failures with no logging — DEK corruption triggers silent Gemini re-call consuming AI credits | `src/services/ai/aiService.ts:546–555` | Split the catch: log `logger.warn` with meetingId and error.message before falling back to `[]` |
| NSF6 | `listBookings` — all three PII decrypt failures (`guestName`, `guestEmail`, `guestNote`) swallowed with empty `.catch(() => "")` | `src/services/scheduling/bookingManagementService.ts:99–107` | Mirror the `logger.warn` pattern used for `guestEmail` in `jobProcessor.ts` on all three fields |
| NSF7 | `guestName` decrypt failure in booking reminder job has empty `.catch(() => "Guest")` — asymmetric with `guestEmail` which logs properly | `src/worker/jobProcessor.ts:419–428` | Add the same `logger.warn` pattern used for `guestEmail` to the `guestName` catch |
| NSF8 | Global `QueryClient mutations.onError` fires alongside hook-level `onError` — every mutation failure shows two toast notifications | `crelyzor-frontend/src/lib/queryClient.ts:23–26` | Remove the global `onError` from `mutations`; each hook already defines its own error handler |
| NSF9 | `ErrorBoundary.componentDidCatch` does nothing in production — React crashes are completely invisible to the engineering team | `crelyzor-frontend/src/components/ErrorBoundary.tsx:27–33` | Remove `import.meta.env.DEV` guard from `console.error`, or add a remote error reporting call |
| NSF10 | `useNotificationSocket` `onmessage` empty catch swallows all processing errors (null pointer in `qc.setQueryData`, invalidation failures) alongside malformed JSON | `crelyzor-frontend/src/hooks/useNotificationSocket.ts:82–98` | Narrow the guard to only the `JSON.parse` call; handle message-processing logic outside the catch |

### HIGH — Performance

| # | Issue | File | Recommendation |
|---|-------|------|----------------|
| NPerf1 | `exportContacts` decrypts 500 contacts sequentially (one at a time) inside a for loop — seconds of blocking on large exports | `src/services/cardService.ts:789–807` | Parallelize: `await Promise.all(batch.map(async (c) => decrypt all fields))` |
| NPerf2 | `importContactsFromCsv` encrypts each CSV row sequentially — same pattern as NPerf1 | `src/services/cardService.ts:875–923` | Parallelize: collect all valid rows, then `await Promise.all(validRows.map(...))` |
| NPerf3 | `removeExistingRecallDeployJobs` scans the entire Bull queue (all states) to find one job — OOM risk when completed jobs accumulate | `src/services/meetings/meetingService.ts:1142–1165` | Use deterministic jobId: `queue.getJob('recall-bot-${meetingId}')` then `job.remove()` — no scan needed |
| NPerf4 | `processTranscriptWithAI` has no guard against the meeting's transcription status — concurrent retries fire 3 Gemini calls unconditionally when `existingTasks.length === 0` | `src/services/ai/aiService.ts:507–618` | Check `meeting.transcriptionStatus === COMPLETED` before the pipeline; add circuit breaker |

### HIGH — DB Layer

| # | Issue | File | Recommendation |
|---|-------|------|----------------|
| NDB1 | `createTask` issues `prisma.task.create` then a separate `prisma.task.update` for GCal event ID outside any transaction — GCal-synced tasks can be left without `googleEventId` on failure | `src/controllers/taskController.ts:274–313` | Wrap both writes in `prisma.$transaction`; same pattern applies in `createStandaloneTask` |
| NDB2 | `slotService.getSchedulingProfile` `EventType.findMany` has no `take` limit | `src/services/scheduling/slotService.ts:293–314` | Add `take: 50` |
| NDB3 | `exportService` loads `MeetingTranscript` with `segments: { take: 5000 }` — OOM risk on long recording exports | `src/services/exportService.ts:18–36` | Stream or chunk transcript segment decryption rather than loading 5000 rows at once |
| NDB4 | `reorderTasks` issues N individual `tx.task.update` calls — malicious payload with thousands of IDs hits DB N times inside a transaction | `src/controllers/taskController.ts:761–787` | Add a `take` cap in the Zod validator (e.g., `z.array(z.string().uuid()).max(200)`) |

### HIGH — API Contract

| # | Issue | File | Recommendation |
|---|-------|------|----------------|
| NAPI1 | `getSummary` response spreads raw Prisma row — `isDeleted`, `deletedAt` and encrypted `Bytes` fields exposed to frontend | `src/controllers/aiController.ts:61–64` | Return explicit shape: `{ id, meetingId, summary: summaryText, keyPoints }` |
| NAPI2 | `smaService.ts` double-unwrap: `apiClient` already unwraps the envelope, then `unwrap()` is called again on the result — works by accident, breaks silently on any shape change | `crelyzor-frontend/src/services/smaService.ts` (all `unwrap()` call sites) | Remove the `unwrap()` helper; use `apiClient.get<T>()` directly since it already unwraps |
| NAPI3 | `getSubtasks` (`GET /sma/tasks/:id/subtasks`) applies `take: 100` but returns no `hasMore`/`total` | `src/controllers/taskController.ts:808` | Return `{ subtasks, hasMore: rawSubtasks.length === 100 }` |
| NAPI4 | `getCardMeetings` applies `take: 50` but returns array directly with no `hasMore`/`total` | `src/services/cardService.ts:1087` | Return `{ meetings, hasMore: meetings.length === 50 }` |
| NAPI5 | `PATCH /sma/meetings/:id/transcript/segments/:segmentId` has no rate limit | `src/routes/smaRoutes.ts:77` | Add `userRateLimit(120, 60 * 60 * 1000, "sma:patch-segment")` |

### HIGH — Frontend Quality

| # | Issue | File | Recommendation |
|---|-------|------|----------------|
| NFQ1 | `useSummary` and `useTasks` poll indefinitely when data is null — meetings where AI never completes generate sustained network traffic forever | `crelyzor-frontend/src/hooks/queries/useSMAQueries.ts:30–34,74–80` | Add a `pollCount` ref cap (≤15 attempts) to both hooks |
| NFQ2 | `ShareSheet.tsx` `setTimeout` for copy feedback discards the timer ID — memory leak if sheet unmounts during the 2s window | `crelyzor-frontend/src/pages/meeting-detail/ShareSheet.tsx:67` | Store in `useRef`, clear in cleanup `useEffect` |
| NFQ3 | Bookings page `isError` shows no retry button | `crelyzor-frontend/src/pages/bookings/index.tsx:371–374` | Destructure `refetch`, add `<Button onClick={() => refetch()}>Try again</Button>` |
| NFQ4 | CardAnalytics `isError` shows no retry button | `crelyzor-frontend/src/pages/card-analytics/CardAnalytics.tsx:140–148` | Same pattern as NFQ3 |
| NFQ5 | CardContacts `isError` shows no retry button | `crelyzor-frontend/src/pages/card-contacts/CardContacts.tsx:399–404` | Same pattern as NFQ3 |
| NFQ6 | Search page never destructures `isError` — API failure shows blank page with no message | `crelyzor-frontend/src/pages/search/Search.tsx:119` | Add `isError` to destructure; render error message when `debouncedQ.length >= 2 && isError` |
| NFQ7 | `SetupPage.tsx` hardcodes the query key string `['username-check', ...]` — not in `queryKeys.ts`, cannot be invalidated | `crelyzor-frontend/src/pages/setup/SetupPage.tsx:42` | Add `queryKeys.users.usernameCheck(username)` to `queryKeys.ts` and use it here |

### MEDIUM — (selected highest impact)

| # | Issue | File | Recommendation |
|---|-------|------|----------------|
| NM1 | `bookingManagementService` `declineBooking`/`cancelBooking` do `findFirst` after `updateMany` — if `updateMany` matched 0 rows, `findFirst` returns null silently | `src/services/scheduling/bookingManagementService.ts:471–477,521–525` | Add null-check; throw `AppError("Booking not found", 404)` if findFirst returns null |
| NM2 | `apiClient.requestForm` and `requestText` have no 401 refresh-and-retry — recording uploads and CSV exports that hit token expiry immediately fail instead of transparently refreshing | `crelyzor-frontend/src/lib/apiClient.ts:194–253` | Apply the same refresh-and-retry block from `request()` to both `requestForm` and `requestText` |
| NM3 | `aiService.ts` MeetingTranscript fetched with no `select` in `generateSummary` — pulls large encrypted `fullText` Bytes column for all fields | `src/services/ai/aiService.ts:511–512` | Add `select: { id: true, fullText: true }` |
| NM4 | `aiController.ts` same over-fetch on `regenerateSummary` and `regenerateTitle` | `src/controllers/aiController.ts:85–91,129–134` | Add `select: { fullText: true }` on both transcript fetches |
| NM5 | Phase 5 backfill script has no per-row error recovery — one bad row crashes the entire migration | `src/scripts/phase5Backfill.ts` (all backfill functions) | Wrap inner loop body in try/catch; log failure with `row.id` and continue; report total failure count |
| NM6 | `deductTranscription` call has no error logging on failure — usage counter drifts silently from actual consumption | `src/services/transcription/transcriptionService.ts:214–216` | Add `.catch((err) => logger.warn("Failed to deduct transcription usage", { userId, actualMinutes, error }))` |
| NM7 | Schema: 17 models missing `isDeleted`/`deletedAt` (previously tracked as S4 — still outstanding) | `prisma/schema.prisma` | `Session`, `LoginHistory`, `RefreshToken`, `OAuthAccount`, `MeetingParticipant`, `TranscriptSegment`, `MeetingSpeaker`, `MeetingAIContent`, `AskAIConversation`, `AskAIMessage`, `GCalSyncState`, `CardView`, `UserUsage`, `Subscription`, `AdminUser`, `AdminInvite`, `MeetingStateHistory` |
| NM8 | `useUpdateProfile` mutation has no `onError` or success toast — silent failure if Settings page caller forgets to handle it | `crelyzor-frontend/src/hooks/queries/useUserQueries.ts:6–14` | Add `onSuccess: () => toast.success('Profile updated')` and `onError: () => toast.error('Failed to update profile')` |
| NM9 | `console.warn('[smaService] Malformed SSE line...')` in production SSE path | `crelyzor-frontend/src/services/smaService.ts:466` | Gate behind `import.meta.env.DEV` or replace with a no-op |
| NM10 | Hardcoded `http://localhost:4000/api/v1` as fallback in `crelyzor-public` (2 files) | `crelyzor-public/src/lib/api.ts:54`, `crelyzor-public/src/app/schedule/[username]/[slug]/page.tsx:52` | Use `process.env.NEXT_PUBLIC_API_URL` with no localhost fallback; fail loudly in production |

---

## Previously Skipped Issues (still outstanding — as expected)

| # | Issue | Reason Skipped |
|---|-------|---------------|
| S1 | taskController — full arch refactor to extract service layer | Major restructuring beyond review scope |
| S2 | aiController — full arch refactor to extract service layer | Same |
| S3 | Google OAuth CSRF `state` parameter | Full OAuth flow changes required |
| S4 | 19 models missing `isDeleted`/`deletedAt` (schema agent now confirms 17) | Schema migration required |
| S5 | Transcript list virtualization (`react-virtual`) | UI library integration out of scope |
| S6 | GCS recording ownership at bucket ACL level | Requires GCS IAM changes |

---

## Issue Count Summary

| Category | Fixed ✅ | Still Present ❌ | Regressions ⚠️ | New 🆕 |
|----------|---------|----------------|----------------|--------|
| Critical | 5 | 2 (C5, H3) | 0 | 5 |
| High | 24 | 0 | 0 | ~38 |
| Medium | 13 | 0 | 0 | ~18 |
| Low | 2 | 0 | 0 | ~8 |
| **Total** | **44** | **2** | **0** | **~69** |

---

## Recommended Next Action

**Run `/crelyzor-review` again.** Fix priority order:

**Fix immediately (block on these):**
1. NC1 — `submitContact` PII leak on public endpoint
2. NC2 — `createNotification` empty `.catch(() => {})`
3. NC3 — `MeetingSpeaker` soft delete gaps (3 service files)
4. NC4 — Task write-path TOCTOU (`isDeleted: false` on 5 `task.update` calls)
5. NC5 — `tagService.verifyContactOwnership` missing `isDeleted` on contact row
6. NSec1 — `getCardMeetings` missing `createdById` ownership scope
7. ❌C5 — `shareController` shortId parse still returning 400 (apply the fix that was supposedly made)

**Fix before next review:**
- NSF1/NSF2 — KMS error wrapping and startup validation
- NSF3 — Redis error event handler
- NSF4–NSF10 — Silent failure chain across AI, booking, and frontend
- NPerf1/NPerf2 — Sequential encrypt/decrypt loops in card service
- NPerf3 — Bull queue full scan
- NFQ1 — Unbounded polling in `useSummary`/`useTasks`
- NAPI1/NAPI2 — getSummary leaking Prisma fields + smaService double-unwrap
- NM5 — Phase 5 backfill per-row error recovery (critical for the current migration)
