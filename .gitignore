# Sub-repos — tracked independently on GitHub
website/
crelyzor-public/
crelyzor-frontend/
crelyzor-backend/
crelyzor-admin/


# Dependencies
node_modules/

# System
.DS_Store
*.log

# Package manager
.pnpm-store/

# Editor
.vscode/

# Local Claude settings (keep skills/agents/settings.json but not local overrides)
.claude/settings.local.json

# Environment secrets — never commit
.env.local
.env.prod
.env.staging

# Internal docs — pricing, specs, pitch deck, reviews (not for public repo)
docs/internal/
